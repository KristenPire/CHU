# 2023905492 — Doris

## Student Info

| Field | Value |
|---|---|
| Repository | https://gitee.com/yXVZEyyxf/rpg-2023905492 |
| Student | 2023905492 — Doris (陈怡彤) |
| Game Score | 64 / 100 |
| Deadline status | ON TIME |
| Compilation | PASS (cmake) |

---

## Section 1 — Subject Requirements: 22 / 30

### 1.1 Compilation
PASS (cmake)

### 1.2 Required Classes: 12 / 15

| Class | Score | Notes |
|---|---|---|
| Character | 3 / 3 | `Character.cpp` implements constructor, all getters, `isAlive()`, `takeDamage()`, `heal()`, and `operator<<` correctly. |
| Item | 3 / 3 | `Item.cpp` implements constructor, getters, `operator==`, and `operator<<` correctly. |
| Inventory | 3 / 3 | `Inventory.cpp` implements `addItem()`, `size()`, `isEmpty()`, `operator<<`, and canonical form. |
| Game + run() | 2 / 3 | `Game.hh` and `Game.cpp` present; `run()` exists but contains the full game loop inline with no delegation to helpers. |
| End condition | 1 / 3 | A win/loss path exists but the termination message is minimal and the condition is not clearly separated from the loop. |

### 1.3 Repository & README: 6 / 10

| Item | Score | Notes |
|---|---|---|
| Repo is public on Gitee | 2 / 2 | |
| README.md exists | 2 / 2 | Present at root. |
| Student ID listed | 2 / 2 | ID appears in README. |
| Game description | 0 / 2 | Description is too brief to convey the story or setting meaningfully. |
| Enemies, items, end condition listed | 0 / 2 | Not listed in README. |

### 1.4 Game Mechanics: 4 / 5

| Item | Score | Notes |
|---|---|---|
| Hero takes damage and can die | 2 / 2 | `Game.cpp:45` — `takeDamage()` called in combat loop; hero death triggers game-over branch. |
| At least one enemy | 2 / 2 | `Game.cpp:30` — enemy defined inline with name, HP, and ATK. |
| At least one usable item | 0 / 1 | Items exist in the inventory but no `use` action is wired to a visible effect during gameplay. |

---

## Section 2 — Clean Code: 24 / 40

### 2.1 Function Size & Focus: 8 / 12
`Character.cpp` and `Item.cpp` functions are short and single-purpose. `run()` in `Game.cpp` handles input, combat resolution, and output in one block of ~80 lines without helper extraction; the combat logic and status printing should be separated. `Inventory.cpp` functions are well-scoped.

### 2.2 Canonical Form: 7 / 10
`Inventory` uses a raw pointer to a `std::vector<Item>`. Copy constructor performs a deep copy (`Inventory.cpp:10`), copy-assignment includes a self-assignment guard (`Inventory.cpp:15`), and the destructor frees the allocation (`Inventory.cpp:24`). Minor issue: the copy-assignment does not reset `m_items` to `nullptr` before the `delete` call in an exception path, which is a low-risk but technically incomplete guard.

### 2.3 Naming: 6 / 10
Member variables follow a consistent `m_` prefix throughout. Function names in `Character.cpp` and `Inventory.cpp` are descriptive. `Game.cpp` introduces some generic local names (`tmp`, `choice`) and a boolean variable named `end` rather than `isGameOver`, which reduces clarity in the game loop.

### 2.4 Style & Readability: 3 / 8
Indentation is consistent in `Character.cpp`, `Item.cpp`, and `Inventory.cpp`. `Game.cpp` contains several magic numbers (enemy HP, damage values, healing amounts) scattered without named constants, and two commented-out blocks remain in the final submission. Blank line usage is irregular within `run()`.

---

## Section 3 — Operator Overloading: 7 / 10

### 3.1 Required Operators: 6 / 8

| Operator | Score | Notes |
|---|---|---|
| operator<< (Character) | 2 / 2 | `Character.cpp:55` — prints name, HP, ATK, DEF. Correct. |
| operator== (Item) | 2 / 2 | `Item.cpp:23` — compares name, type, and value. Correct. |
| operator<< (Item) | 1 / 2 | `Item.cpp:28` — prints name and value; type is omitted from the output. |
| operator<< (Inventory) | 1 / 2 | `Inventory.cpp:43` — iterates and prints items, but format is bare with no delimiter or index. |

### 3.2 Bonus Overloads: 1 / 2
`operator+=` defined on `Inventory` to add an item (`Inventory.cpp:60`). Appropriate and improves call-site readability. No further bonus overloads.

---

## Section 4 — Heap / Dynamic Memory: 7 / 10

> Mode: RAW POINTERS

`Inventory` allocates `new std::vector<Item>()` in its constructor. Each `new` is paired with a `delete` in the destructor and copy-assignment. Copy constructor allocates fresh memory before copying (`Inventory.cpp:10`), avoiding shallow copy. Pointers are not nulled after deletion in the destructor, leaving a dangling pointer for the window between the `delete` call and the object's actual destruction — low-severity but a deduction. No double-free detected.

---

## Section 5 — Inheritance: 4 / 10

> Inheritance used: YES

### 5.1 Design Quality: 2 / 5
An `Enemy` class inherits from `Character` (`Enemy.hh`). The is-a relationship is valid. The base class interface is thin — only one method is overridden — and the hierarchy adds limited value over a plain `Character` with a type field. The design is functional but not well-motivated.

### 5.2 Technical Correctness: 2 / 5

| Item | Score | Notes |
|---|---|---|
| Virtual destructor | 0 / 2 | `Character.hh` destructor is not declared `virtual`; deleting an `Enemy` via a `Character*` is undefined behavior. |
| override keyword | 1 / 1 | `Enemy.cpp` uses `override` on the overriding method. |
| No object slicing | 1 / 2 | `Enemy` objects are stored in a local variable by value in `Game.cpp:30`, not by pointer, which causes slicing if passed to a function expecting `Character&`. |

---

## Scoring Summary

| Section | Score | Max |
|---|---|---|
| 1. Subject Requirements | 22 | 30 |
| 2. Clean Code | 24 | 40 |
| 3. Operator Overloading | 7 | 10 |
| 4. Heap / Dynamic Memory | 7 | 10 |
| 5. Inheritance | 4 | 10 |
| **Total** | **64** | **100** |

---

## AI Usage

**AI_USAGE: 2 / 5** — The core classes are clean and consistent, but `Game.cpp` is notably messier with magic numbers and dead code, which is inconsistent with AI-generated work; overall the mix of polished and rough sections suggests partial AI assistance at most.

- `Character.cpp:1–61` — uniform style and no variation across all methods (raise↑)
- `Game.cpp:45` — magic numbers for damage and HP values alongside commented-out blocks are organic student signals (lower↓)
- `Inventory.cpp:8–25` — canonical form correct on first attempt with no common beginner errors (raise↑)
- `Game.cpp:30` — `Enemy` stored by value rather than pointer, a typical student oversight inconsistent with AI output (lower↓)
