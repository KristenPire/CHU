# 2024902259 — Simon

## Student Info

| Field | Value |
|---|---|
| Repository | https://gitee.com/simon1020/rpg-2024902259.git |
| Student | 2024902259 — Simon |
| Game Score | |
| Deadline status | ON TIME (last commit: 2026-04-24 11:32 +0800) |
| Compilation | PASS (cmake) |

---

## Section 1 — Subject Requirements: 29 / 30

### 1.1 Compilation
PASS
> cmake + cmake --build succeed cleanly with no warnings on AppleClang 16. Build artifacts (build/*.o, build/rpg.exe) were committed to the repo and should be in .gitignore, but this does not affect the compilation score.

### 1.2 Required Classes: 15 / 15

| Class | Score | Notes |
|---|---|---|
| Character | 3 / 3 | Character.cpp implements all required methods; operator<< at Character.cpp:56 prints name, HP/maxHP, ATK, DEF. Two extra methods (adjustAtk, adjustDef) added per the "you may add more methods here" comment in the header — acceptable extension. |
| Item | 3 / 3 | Item.cpp implements constructor, all getters, operator== at Item.cpp:27 (name+type+value), operator<< at Item.cpp:32. |
| Inventory | 3 / 3 | Inventory.cpp implements addItem, size, isEmpty, operator<<. Canonical form present: copy ctor (= default, correct since STL vector), copy-assign (explicit with self-assign guard at Inventory.cpp:10-16), destructor (= default). Extra helpers removeFirst and getContents added. |
| Game + run() | 3 / 3 | Game.hh:11 declares run(); Game.cpp:1105 implements it. main.cpp is unmodified — exactly the provided skeleton. |
| End condition | 3 / 3 | outroWin() at Game.cpp:698 fires when boss is defeated; outroLose() at Game.cpp:718 fires when player HP reaches 0. Both display a clear message and a run summary. No infinite-loop risk. |

### 1.3 Repository & README: 9 / 10

| Item | Score | Notes |
|---|---|---|
| Repo is public on Gitee | 2 / 2 | Accessible and cloned successfully. |
| README.md exists | 2 / 2 | Present at root, 101 lines. |
| Student ID listed | 2 / 2 | "2024902259" appears clearly at README.md:4. |
| Game description | 2 / 2 | README describes the One Piece-themed undersea prison breakout with role selection, floor progression, and boss fight. |
| Enemies, items, end conditions listed | 1 / 2 | Six enemies listed with names; five items listed with types. End conditions listed. Deduct 1: the README submission checklist (lines 96-101) has several unchecked boxes ([ ]) — the "Fresh clone builds successfully" and test paths remain unchecked, suggesting the README was not reviewed before submission. |

### 1.4 Game Mechanics: 5 / 5

| Item | Score | Notes |
|---|---|---|
| Hero takes damage and can die | 2 / 2 | takeDamage() wired into combat at Game.cpp:882, 891, 905; death check at Game.cpp:759 triggers outroLose path. |
| At least one enemy | 2 / 2 | Six distinct enemies created via makeGrunt() and makeBoss(); combatEncounter drives actual fight logic. |
| At least one usable item | 1 / 1 | Potions consumed in combat at Game.cpp:816-824; removeFirst removes them from inventory; heal applied. |

---

## Section 2 — Clean Code: 29 / 40

### 2.1 Function Size & Focus: 8 / 12

Most helpers are short and focused: `floorName` (8 lines), `makeGrunt` (14 lines), `potionHealBonus` (1 line), `fleeSuccessRollCap` (1 line), `roleStyleLine` (10 lines), all the event functions (10–30 lines each), and `run()` itself (Game.cpp:1105–1161, 57 lines) reads cleanly as a high-level orchestrator. The role-passive accessors are a particularly good pattern — each extracts a single role-dependent value into its own named function, making `combatEncounter` readable.

The main drag is `combatEncounter` at Game.cpp:750–911 (163 lines). It handles player choice parsing, boss phase transitions, double-strike logic, brace mechanics, relic curse application, Luffy's post-skirmish heal, flee resolution, and loot — all in a single function. Extracting `handlePlayerTurn()` (choice 1–4 dispatch) and `handleEnemyTurn()` (boss vs grunt strike, phase checks) would halve the length with no logic change. `chooseRole()` at Game.cpp:401–488 (88 lines) is also large, mixing UI display, `rand` roll, character construction, and spotlight call; the construction block alone (lines 462–487) could be a one-argument factory. Score drops from 12 to 8 for these two oversized functions.

### 2.2 Canonical Form: 8 / 10

`Inventory` uses `std::vector<Item>` and has all three canonical members. The default copy constructor (Inventory.cpp:8) and default destructor (Inventory.cpp:18) are correct — the vector handles deep copy automatically. The copy-assignment operator at Inventory.cpp:10–16 is explicit and correct: self-assignment guard present, assignments `items_ = other.items_`, returns `*this`.

`Game` stores `Character player_` and `Inventory bag_` by value with no raw pointers, so it has no resource-management obligation; correctly left without explicit canonical form. `Character` likewise. The one note: `Inventory.cpp` includes `<map>`, `<string>`, and `<tuple>` that are only used inside `operator<<` — these belong in the `.cpp` only, which is correct, no issue. Deduct 2: the copy constructor being `= default` is technically correct for STL-only classes, but the rubric expects the student to demonstrate awareness by at minimum implementing an explicit one for `Inventory`. Given it really is correct as written, this is a borderline call — taking the lower band per instructions.

### 2.3 Naming: 9 / 10

Naming is consistently good. Private members use trailing underscore convention throughout (`name_`, `hp_`, `maxHp_`, `role_`, `bounty_`, `bossDefeated_`). Boolean members are named with clear is/has prefix patterns (`isAlive()`, `isEmpty()`, `roninFirstAttackBonus_`, `relicActive_`, `bossDefeated_`). Free functions in the anonymous namespace are descriptive (`floorName`, `enemyTitleForFloor`, `makeGrunt`, `makeBoss`). Game methods read like actions (`chooseRole`, `initStarterPack`, `oathBranch`, `bossApproachBranch`, `combatEncounter`, `maybeLootAfterVictory`). Role enum values are clear (`RoleKind::Luffy`, `RoleKind::Zoro`, etc.). One minor point: `c` is used as the local name for menu choice results throughout (Game.cpp:312, 339, 509, 576, 601) — fine in short functions but slightly terse; `choice` or `menuChoice` would be clearer. Deduct 1 for this pattern.

### 2.4 Style & Readability: 4 / 8

Indentation is consistent (4-space, clean brace style throughout). No mixed tabs/spaces. No commented-out dead code. However, magic numbers are scattered throughout `Game.cpp` with no named constants: loot chance percentages (`22`, `48`, `24`, `34`, `46`, `47`), bounty deltas (`85000`, `65000`, `80000`, `50000`, `120000`, `45000`, `90000`, `70000`, `35000`), heal amounts (`7`, `6`, `4`, `3`, `2`), boss phase thresholds (`0.70`, `0.40`), combat parameters. None of these are explained by name. With named constants — `kBasePotion1Cost`, `kEliteLootChance`, `kBossPhase1Threshold` — the numbers would carry meaning on first read. The pattern is consistent and affects nearly every function, so the deduction is significant. The `(void)roomIndex` and `(void)floorIndex` suppressors (Game.cpp:1063, Game.cpp:997) are acceptable practice. Build artifacts committed (build/*.o, build/rpg.exe) suggest no `.gitignore` was set up — minor style issue outside the code itself.

---

## Section 3 — Operator Overloading: 10 / 10

### 3.1 Required Operators: 8 / 8

| Operator | Score | Notes |
|---|---|---|
| operator<< (Character) | 2 / 2 | Character.cpp:56 — prints name, HP/maxHP, ATK, DEF in compact format. Correct return type. |
| operator== (Item) | 2 / 2 | Item.cpp:27 — compares name_, type_, and value_. Returns bool. Not trivially true/false. |
| operator<< (Item) | 2 / 2 | Item.cpp:32 — prints name, type label (via anonymous-namespace helper), and value in "[Type +N]" format. |
| operator<< (Inventory) | 2 / 2 | Inventory.cpp:51 — handles empty case explicitly; aggregates duplicates using std::map<tuple> for a clean "item x2" display. Correct return type. |

### 3.2 Bonus Overloads: 2 / 2

`Inventory::removeFirst` uses `operator==` on Item internally (Inventory.cpp:38), which is correct and meaningful reuse. The `operator<<` on `Inventory` uses `operator<<` on `Item` (line 73 via the proxy). Beyond these uses, no additional overloaded operators (e.g., `operator+=` on Inventory) were added. However, the anonymous-namespace `typeLabel` helper in Item.cpp that enables a readable `operator<<` output is a clean design choice. Awarding the 2 bonus points for the unusually careful `operator<<` on Inventory (map-based deduplication is well above the minimum) and clean helper design.

---

## Section 4 — Heap / Dynamic Memory: 8 / 10

> Mode: STL ONLY

`Inventory` uses `std::vector<Item> items_` with no raw pointers anywhere in the codebase. The canonical form is present and correct (see §2.2). Large objects: `Character` and `Inventory` are passed by reference in `combatEncounter` (Game.cpp:750 — `Character& enemy`). `bag_.getContents()` at Game.cpp:798 returns a copy of the vector, which is used to filter potions; this copy is made intentionally to iterate without modifying the backing store, and the copy is a `std::vector<Item>` of small value objects — acceptable. However, `getContents()` is called every combat turn and returns by value, which is a gratuitous copy in a loop (Game.cpp:798 inside the `while` combat loop). `Inventory` also stores `std::vector<Item>` without any use of `reserve()` or size estimation. Deduct 2 for the repeated by-value copy of the full inventory vector inside the hot combat loop.

---

## Section 5 — Inheritance: 0 / 10

> Inheritance used: NO

No inheritance anywhere in the codebase. `Character` is used directly for both the player and all enemies — enemies are constructed as plain `Character` objects via `makeGrunt()` and `makeBoss()` factory functions. Score: 0/10.

---

## Scoring Summary

| Section | Score | Max |
|---|---|---|
| 1. Subject Requirements | 29 | 30 |
| 2. Clean Code | 29 | 40 |
| 3. Operator Overloading | 10 | 10 |
| 4. Heap / Dynamic Memory | 8 | 10 |
| 5. Inheritance | 0 | 10 |
| **Total** | **76** | **100** |

---

## AI Usage

**AI_USAGE: 4 / 5** — The entire project was submitted in a single large commit (1161-line Game.cpp, all files at once), the code has a uniformly polished style with zero variation between files, `static_assert` for compile-time ASCII frame width verification appears in Portrait.cpp:19–24 (a usage pattern highly unlikely to originate independently at this course level), and the `operator<<` on Inventory uses a `std::map<std::tuple<...>>` deduplication approach that is idiomatic but well beyond what students at this level typically produce unprompted.

- `Portrait.cpp:19` — `static_assert(sizeof(kWfTop1) - 1 == 42, ...)` with six identical assertions for frame rows — compile-time width validation with `sizeof` trick is a strong AI signal↑
- `Inventory.cpp:58` — `std::map<Key, int>` with `std::tuple<std::string, int, int>` as key for deduplication in `operator<<` — advanced STL idiom well above course level↑
- `Game.cpp:750` — `combatEncounter` is 163 lines of complex logic that is internally consistent and bug-free but also monolithic in exactly the way AI code tends to be↑
- `Game.cpp:1` — no Doxygen-style comments and presence of scattered magic numbers (not cleaned up) are weak signals pointing slightly toward genuine student work↓
