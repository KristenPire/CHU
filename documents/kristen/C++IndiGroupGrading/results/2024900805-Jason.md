# 2024900805 — Jason

## Student Info

| Field | Value |
|---|---|
| Repository | https://gitee.com/chen-xuanrui66/jason--2024900805 |
| Student | 2024900805 — Jason |
| Game Score | |
| Deadline status | ON TIME (last commit: 2026-04-24 15:53:42 +0800) |
| Compilation | PASS (cmake) |

---

## Section 1 — Subject Requirements: 19 / 30

### 1.1 Compilation
PASS (cmake)
> CmakeLists.txt has non-standard casing but cmake found and built cleanly; no external libraries used.

### 1.2 Required Classes: 12 / 15

| Class | Score | Notes |
|---|---|---|
| Character | 2 / 3 | Character.cpp exists; constructor, all getters, isAlive(), takeDamage(), heal() all correct. operator<< is not declared in Character.hh and not implemented anywhere — missing. |
| Item | 2 / 3 | Item.cpp exists; constructor and all getters correct. operator== and operator<< are absent from both Item.hh and Item.cpp. |
| Inventory | 3 / 3 | Inventory.cpp exists; addItem(), size(), isEmpty(), removeItem(), getItem() all work; canonical form (copy ctor, copy-assign with self-assignment guard, destructor) correctly implemented using raw pointer to vector. |
| Game + run() | 3 / 3 | Game.hh and Game.cpp exist; public run() present; main.cpp unmodified. |
| End condition | 2 / 3 | Game ends with "VICTORY!" on defeating the Dragon (Game.cpp:171) and "GAME OVER" on death (Game.cpp:173). Flee path sets gameOver=true without victory=true, which produces "GAME OVER" with no escape message — acceptable but bare. |

### 1.3 Repository & README: 2 / 10

| Item | Score | Notes |
|---|---|---|
| Repo is public on Gitee | 2 / 2 | Accessible and cloned successfully. |
| README.md exists | 0 / 2 | README.md exists but is 100% the default Gitee template with no student-written content. |
| Student ID listed | 0 / 2 | The title line "Jason -2024900805" appears but only as the Gitee auto-generated repo name — no deliberate listing of the student ID in context. Both README.md and README.en.md are identical stock templates. |
| Game description | 0 / 2 | No game description. All body text is Gitee boilerplate. |
| Enemies, items, end condition listed | 0 / 2 | None described. |

### 1.4 Game Mechanics: 5 / 5

| Item | Score | Notes |
|---|---|---|
| Hero takes damage and can die | 2 / 2 | Game.cpp:111 — player.takeDamage() called each enemy turn; Game.cpp:148-150 — !player.isAlive() triggers gameOver. |
| At least one enemy | 2 / 2 | Three enemies defined in Game.cpp:17-19 (Jason, MIke, Dragon); combat loop drives each encounter. |
| At least one usable item | 1 / 1 | Game.cpp:54-84 — useItem() applies Potion (heal), Weapon (ATK boost), Armor (DEF boost) with inventory removal. |

---

## Section 2 — Clean Code: 28 / 40

### 2.1 Function Size & Focus: 10 / 12

Each function does one thing and is short. `battle()` (Game.cpp:140-161, ~20 lines) loops and delegates to `showStatus()`, `playerTurn()`, `enemyTurn()`, `nextEncounter()` — good high-level structure. `useItem()` (Game.cpp:54-84, ~30 lines) is slightly long but handles three item types inline; an `applyItem()` helper would clean it up. `playerTurn()` (Game.cpp:86-107) mixes menu I/O with combat dispatch but stays under 25 lines. All other helpers (clearScreen, waitEnter, showStatus, attemptFlee, enemyTurn) are 5-10 lines and focused. No deeply nested logic anywhere.

### 2.2 Canonical Form: 8 / 10

Inventory uses a raw `std::vector<Item>*` and implements the Rule of Three correctly. Copy constructor (Inventory.cpp:7-9) does `new std::vector<Item>(*other.items)` — correct deep copy. Copy-assignment (Inventory.cpp:11-16) checks `this == &other`, deletes old, allocates new — correct. Destructor (Inventory.cpp:18-20) calls `delete items` — correct. No other resource-owning classes. Deduct 2 pts: wrapping a vector in a raw pointer purely to satisfy a canonical-form exercise is an unusual design choice — the vector manages its own memory already and the pointer adds risk without benefit; a direct `std::vector<Item>` member would be cleaner and idiomatic.

### 2.3 Naming: 8 / 10

Names are generally clear and readable: `currentEncounter`, `gameOver`, `victory`, `waitEnter()`, `showStatus()`, `attemptFlee()`, `nextEncounter()` all read well. Minor issues: Game.cpp:18 has `"MIke"` (inconsistent casing for an enemy name), `c` (Game.cpp:89) for a menu choice is a single-letter variable outside a loop counter. The comment on Game.cpp:16 says "Enemies exactly as you defined" — seems like leftover from a template or AI prompt. No actively confusing names.

### 2.4 Style & Readability: 2 / 8

`using namespace std;` at file scope in Game.cpp:6 — a style anti-pattern in a C++ course. Magic numbers scattered throughout: player stats `50, 8, 2` (Game.cpp:9), enemy stats (Game.cpp:17-19), flee chance `7` in `(rand() % 10) < 7` (Game.cpp:51), item values `10, 15, 5, 4` — none are named constants. The comment `// Enemies exactly as you defined` (Game.cpp:16) and `// Give loot exactly as defined` (Game.cpp:116) appear to be residue from a prompt or template and add no value. `// Starting item` (Game.cpp:21) is a low-value comment. Overall the code has readable indentation and no dead commented-out blocks, but the magic numbers and namespace pollution are consistent style issues.

---

## Section 3 — Operator Overloading: 0 / 10

### 3.1 Required Operators: 0 / 8

| Operator | Score | Notes |
|---|---|---|
| operator<< (Character) | 0 / 2 | Not declared in Character.hh and not implemented in Character.cpp or anywhere else. |
| operator== (Item) | 0 / 2 | Not declared in Item.hh and not implemented in Item.cpp or anywhere else. |
| operator<< (Item) | 0 / 2 | Not declared in Item.hh and not implemented in Item.cpp or anywhere else. |
| operator<< (Inventory) | 0 / 2 | Not declared in Inventory.hh and not implemented in Inventory.cpp or anywhere else. |

### 3.2 Bonus Overloads: 0 / 2

No additional operator overloads present. The only operator in the codebase is `Inventory::operator=` (copy-assignment), which is a canonical-form requirement, not a bonus.

---

## Section 4 — Heap / Dynamic Memory: 7 / 10

> Mode: RAW POINTERS (pointer-to-vector pattern)

Inventory.hh:21 declares `std::vector<Item>* items` — a raw owning pointer. The canonical form is correctly implemented (see §2.2), so no leaks or double-free. However, `getItem()` (Inventory.cpp:26-28) returns by value without bounds checking on the upper end — only `removeItem()` checks bounds (Inventory.cpp:31). A bad index in `getItem` will throw `std::out_of_range` from vector at runtime, which is acceptable but not explicitly handled. No dangling pointers (pointer is set to a fresh allocation immediately in all paths, and the destructor calls `delete items`). Score reduced from full 10: the raw pointer wrapping an STL container is an unusual pattern that introduces pointer risk without benefit; canonical form is correct but this design choice is awkward (−3).

---

## Section 5 — Inheritance: 0 / 10

> Inheritance used: NO

No inheritance used. Score: 0/10. `std::vector<Character>` is used in Game.hh:25 for enemies, storing all characters by value. There was a natural opportunity to derive an `Enemy` class from `Character`.

---

## Scoring Summary

| Section | Score | Max |
|---|---|---|
| 1. Subject Requirements | 19 | 30 |
| 2. Clean Code | 28 | 40 |
| 3. Operator Overloading | 0 | 10 |
| 4. Heap / Dynamic Memory | 7 | 10 |
| 5. Inheritance | 0 | 10 |
| **Total** | **54** | **100** |

---

## AI Usage

**AI_USAGE: 2 / 5** — The Game.hh helper method decomposition (`nextEncounter`, `playerTurn`, `enemyTurn`, `useItem`, `attemptFlee`, `showStatus`, `clearScreen`, `waitEnter`) is suspiciously complete and well-named for a submission that left all four required operators unimplemented and submitted a blank README, suggesting AI may have scaffolded the structure while the student ran out of time to complete it.

- `Game.hh:14-22` — eight well-named private helpers declared cleanly, consistent with AI-generated scaffolding (raise↑)
- `Game.cpp:16` — comment "// Enemies exactly as you defined" reads as a leftover AI prompt artifact (raise↑)
- `Game.cpp:18` — `"MIke"` typo (lowercase k) in enemy name — an organic student error inconsistent with otherwise clean naming (lower↓)
- `README.md:1-40` — both READMEs are pure default Gitee templates with zero content — suggests the student worked quickly and did not polish or AI-generate the documentation (lower↓)
