# Project Report — Daniel

| | |
|---|---|
| **Student** | 2024903034 — Daniel |
| **Submission** | On time |
| **Final Score** | 95 / 100 |

---

## Overall

A cyberpunk ninja RPG with a polymorphic Event hierarchy, an equipment system, a level-up mechanic, and a gated final boss — one of the most feature-complete submissions in the group. The abstract `Event` base class with pure virtual `trigger()` and three concrete subclasses dispatched polymorphically is exactly the kind of design the course is trying to teach. The main deductions come from magic numbers scattered through the logic, a Rule of Three violation on `Game`, and an incomplete `ItemDiscoveryEvent`.

---

## Sections

**Subject Requirements — 29 / 30**
- All five required classes fully implemented; three AI enemies; weapons and armor equippable via `Player::equipItem()`; proper win/lose conditions
- **README incomplete** (−1) — win condition stated but no regular enemies listed, no items listed, and lose condition not stated

**Clean Code — 32 / 40**
- `run()` (16 lines), `showMenu()` (13 lines), and `handleInput()` switch dispatch are all short and focused; `printHeader()`, `printSeparator()`, `showStatus()` are clean single-purpose helpers
- Canonical form implemented comprehensively on all nine classes including the Event hierarchy, with correct delegation in derived copy-assign
- Consistent `m_` prefix throughout; Event subclass names (`BlessingEvent`, `CurseEvent`, `ItemDiscoveryEvent`) are semantically clear
- **Function size** (−3) — `useItem()` (91 lines) mixes inventory display, input validation loop, and 4-case item dispatch; `battle()` (68 lines) mixes display, input, player action, and enemy action

  A 91-line useItem() that simultaneously shows the inventory, validates input in a loop, and dispatches four item types cannot be understood or modified at the granularity of any single one of those concerns — each should be its own function.

  ```cpp
  // Game.cpp:372 — useItem() opens with inventory display + input loop before any dispatch
  void Game::useItem()
  {
      clearScreen();
      if (m_inventory.isEmpty()) { /* ... */ return; }

      std::cout << m_inventory << std::endl;
      std::cout << "Enter the item number to use, or 0 to exit: ";

      int choice;
      while (true)  // input validation loop — 20+ lines before item dispatch
      {
          if (std::cin >> choice) { /* ... */ break; }
  ```

- **Duplicated input loops** (−2) — three nearly-identical `getValid*Input()` helpers share ~25 lines of logic each — should be one parameterized helper

  Copy-pasting a validation loop three times means a bug in the loop logic (or a desired change to the error message) must be fixed in three places — the common logic should be extracted into a single helper that takes the valid range as parameters.

  ```cpp
  // Game.cpp — three functions with near-identical structure
  // getValidInput()       — for menu choices
  // getValidBattleInput() — for battle choices
  // getValidItemInput()   — for item selection
  // each repeats the same cin >> / cin.clear() / cin.ignore() loop
  ```

- **Magic numbers and style** (−3) — player/enemy stats in `initializeGame()`, event probability `30`, level-up increments in `levelUp()` all use raw literals; `exit(0)` called from `victory()` and `handleInput()` case 6 rather than returning through the stack; `ItemDiscoveryEvent::trigger()` takes `Player&` but never uses it — discovered items are printed but never added to inventory

  `exit(0)` called from a deep function bypasses all destructors and stack unwinding, making it impossible to add cleanup logic later and causing resource leaks in any RAII-managed resources that haven't been closed yet.

  ```cpp
  // Game.cpp:503 — victory() calls exit(0) instead of returning
  void Game::victory()
  {
      printHeader();
      std::cout << "\n=== VICTORY ===" << std::endl;
      std::cout << "You have defeated Kuroha Kurusu!" << std::endl;
      printSeparator();
      exit(0);   // bypasses destructors and stack unwinding
  }

  // Event.cpp:152 — ItemDiscoveryEvent::trigger() never uses its Player& argument
  void ItemDiscoveryEvent::trigger(Player& player)
  {
      // In a real game, we would add the item to the player's inventory
      // For now, we just print a message
      std::cout << "You discovered an item!" << std::endl;
      std::cout << m_item << std::endl;
  }
  ```

**Operator Overloading — 9 / 10**
- All four required operators implemented correctly; `operator<<` on Item prints name, type string, and effect (e.g. "ATK +15") — detailed and readable
- `Event::operator<<` (Event.cpp:39) — bonus overload on a non-required class, prints event name and description; used in `generateEvent()` (−1 for only one bonus overload)

  Two additional points were available for any operator that fits naturally in your code — `operator+=` for healing, `operator-=` for damage, `operator<` for comparing item values, or `operator+` for merging inventories are all common choices in an RPG.

**Heap / Dynamic Memory — 8 / 10**
- Raw pointer mode: Player's `Item*[4]` equipment array is deep-copied correctly in all three canonical members; Inventory's `vector<Item*>` likewise
- **Rule of Three on Game** (−2) — `Game` defines a destructor (deletes Event pointers) but no copy constructor or copy-assignment; any copy of `Game` would double-free all Event objects

  When Game is copied without a copy constructor, both copies share the same Event pointers; when both objects are destroyed, each destructor tries to delete the same pointers, causing undefined behavior (typically a crash).

  ```cpp
  // Game.cpp:28 — destructor deletes all Event pointers
  // (from result file — destructor iterates m_events and deletes each)
  // Game has no copy constructor or copy-assignment operator declared
  // initializeGame() creates events with new:
  m_events.push_back(new BlessingEvent("Ancient Blessing", ...));
  m_events.push_back(new CurseEvent("Dark Curse", ...));
  m_events.push_back(new ItemDiscoveryEvent("Treasure Chest", ...));
  ```

**Inheritance — 9 / 10**
- Event hierarchy: abstract base with pure virtual `trigger(Player&)`, three concrete subclasses each implementing distinct behavior, polymorphic dispatch in `generateEvent()` — genuinely effective
- Character → Player and Character → AI: valid is-a relationships; virtual destructor on Character; `override` keyword used correctly on AI::attack() and all Event subclasses
- **Hollow AI override** (−1) — `AI::attack()` calls `target.takeDamage(getATK())` — functionally identical to `Character::attack()`; the override adds no new behavior

  An override that does exactly what the base class already does adds noise without benefit; the purpose of overriding attack() would be to give the AI distinct behavior (area damage, status effects, multi-hit), and without that, the virtual dispatch mechanism is exercised for no gain.

  ```cpp
  // Character.cpp:290 — AI::attack() is identical to Character::attack()
  void AI::attack(Character& target)
  {
      // Simple AI attack logic
      target.takeDamage(getATK());   // same as Character::attack()
  }
  ```

## Bonus

**Bonus score: 8 / 10**

The Event hierarchy — abstract base class, pure virtual `trigger()`, three concrete subclasses, polymorphic dispatch in `generateEvent()` — is exactly the kind of design thinking the course is trying to teach, and you didn't just write it, you actually wired it up to work. That's the part that matters.

A cyberpunk ninja setting with themed gear like Shuriken and Smoke Bomb, an equipment tracking system, a level-up mechanic, and a gated final boss all add up to one of the most feature-complete games here.

**Clean Code note:** The overall decomposition is good — `run()` is clean, `showMenu()` and `handleInput()` delegate properly, and the three getValid* input helpers show a deliberate instinct for extracting repetition. The Event hierarchy with virtual destructors and `override` keywords is the strongest technical design in the project. The main things to sharpen are merging the three nearly-identical input loops into one parameterized helper, completing the `ItemDiscoveryEvent::trigger()` so discovered items actually enter the inventory, and replacing string-based dispatch in `useItem()` with item-type enum matching.
