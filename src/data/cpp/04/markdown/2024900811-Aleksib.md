# Project Report — Aleksib

| | |
|---|---|
| **Student** | 2024900811 — Aleksib |
| **Submission** | On time |
| **Final Score** | 100 / 100 |

---

## Overall

A atmospheric dungeon crawler with a dual win condition — defeat the boss AND collect three Memory Shards — and a void corruption mechanic that deals 5 damage after every fight, creating constant pressure. The `Enemy : Character` inheritance is semantically sound, the code is clean and well-organized throughout, and the enemy special abilities (life steal, regeneration, defensive behavior) push combat well beyond a basic hit exchange. Magic numbers scattered through the probability logic are the only real rough edge.

---

## Sections

**Subject Requirements — 30 / 30**
- Perfect score; all required classes complete and correct
- Full README with enemy tables, item descriptions, and explicit win/lose conditions
- Three enemies plus a boss; item consumption with visible effects; dual victory condition

**Clean Code — 36 / 40**
- `run()` is a clean 30-line state machine loop; `handleExplore()`, `playerTurn()`, `enemyTurn()` are short and single-purpose
- `Utils` class with `readInt()` and `randomChance()` keeps input handling out of game logic
- Consistent camelCase naming throughout; state variables clearly named (`bossDefeated`, `memoryShards`, `areaLevel`)
- **`handleFight()` too long** (−2) — ~80 lines mixing combat loop management, player choice dispatch, and post-fight transitions

  An 80-line function that handles the combat loop, player input dispatch, void-corruption damage, and state transitions makes it hard to locate any one behavior — splitting into `runCombatLoop()`, `applyPostFightEffects()`, and `resolveFightOutcome()` would make each piece independently readable.

  ```cpp
  // Game.cpp:154 — handleFight() opens the combat loop, dispatches choices,
  // applies void corruption, and transitions state — all inline
  void Game::handleFight() {
      Enemy enemy = (areaLevel == 2 && !bossDefeated) ? createBoss() : createRandomEnemy();
      bool enemyDefending = false;
      while (player.isAlive() && enemy.isAlive()) {
          // ... ~60 lines of dispatch, enemy turn, void damage, state transition ...
          player.takeDamage(5);   // void corruption inline, not in a helper
          std::cout << "The void corrupts you for 5 damage.\n";
      }
  }
  ```

- **Magic numbers** (−2) — encounter probabilities (`60`, `85`), reward probabilities (`40`, `70`, `90`), escape chance (`50`), void damage (`5`) scattered without named constants

  Magic numbers make game balance invisible to readers and fragile to change — when `5` appears as the void damage, a reader cannot tell at a glance whether it is a design constant or an incidental value, and changing the damage requires searching the whole file.

  ```cpp
  // Game.cpp:131 — encounter probability check with bare integers
  int roll = std::rand() % 100;
  if (roll < 60) {          // 60% fight chance — unnamed
      state = GameState::Fight;
  } else if (roll < 85) {   // 25% item chance — unnamed
      giveRandomReward();
  }
  // Game.cpp:203 — void damage also unnamed
  player.takeDamage(5);
  ```

**Operator Overloading — 8 / 10**
- All four required operators correct and complete; type-aware `operator<<` for Item
- No bonus operator overloads (−2)

**Heap / Dynamic Memory — 10 / 10**
- STL only; Inventory canonical form correct; objects passed by reference in helpers; full marks

**Inheritance — 9 / 10**
- `Enemy : public Character` — genuine is-a relationship; Enemy adds `decideAction()`, life steal, and regeneration behavior
- Character has no virtual methods, so the hierarchy provides code reuse but no polymorphic dispatch (−1); no virtual destructor needed since Enemy objects are stored by value

## Bonus

**Bonus score: 9 / 10**

The dual win condition — kill the boss AND collect three Memory Shards — is a smart design choice that makes the endgame feel earned rather than just "reach the final room." The void corruption taking 5 damage after every fight is an elegant tension mechanic: you're always trading resources against progress, which gives the whole run a sense of urgency.

Enemy special abilities (life steal on Echo of Regret, regeneration, defensive behavior) push the combat well beyond a basic hit-exchange loop, and the names — Whispering Wraith, Keeper of the Gate — actually fit the atmosphere you built.

**Clean Code note:**
The code is well-organized and a pleasure to read. The `GameState` enum drives a clean state machine loop, `handleExplore()` and `handleFight()` are clearly separated responsibilities, and small helpers like `checkVictoryCondition()`, `giveRandomReward()`, and `applyItemToPlayer()` keep the logic easy to follow. The `Utils` class with `readInt()` and `randomChance()` is a thoughtful addition that keeps input handling out of the game logic. The one area to improve would be extracting the magic-number probabilities into named constants — the game mechanics are good enough that they deserve to be readable in the code.
