# Project Report — Peter

| | |
|---|---|
| **Student** | 2024900800 — Peter |
| **Submission** | On time |
| **Final Score** | 91 / 100 |

---

## Overall

"Ancient Chinese Fantasy Quest" is a Ming Dynasty 1428 RPG with three mechanically distinct professions (dodge, damage reduction, lifesteal-on-crit), five escalating enemies each with lore, three upgradeable weapons, an EXP/coin economy, and an in-game shop — a complete game in every meaningful sense. The code is architecturally strong with 14 private Game methods and a 5-line `run()`. The main weakness is that the full subclassing infrastructure was set up (virtual method, protected members, currentEnemy pointer) but never completed into actual inheritance.

---

## Sections

**Subject Requirements — 30 / 30**
- Full marks: all five required classes complete; perfect README (10/10) with all 5 enemies listed with stats and lore, all items with values and shop prices, win condition described; three distinct end conditions; all mechanics present

**Clean Code — 36 / 40**
- Outstanding decomposition: `run()` (5 lines), 14 private methods each with clear single responsibility (`showEnemyStory()`, `showVictoryLore()`, `getCurrentWeaponDmg()`, `useSkill()`, `spendExp()`, `shop()`, `selectWeapon()` — all short and focused)
- Leading underscore convention on all private/protected members across all classes; no `using namespace std` anywhere
- **`intro()` and `battle()` focus** (−2) — `intro()` mixes profession display and character selection/initialization; `battle()` handles player turn, enemy turn, and dodge/crit checks inline; splitting these into dedicated sub-methods would complete the decomposition

  A function that both presents UI and mutates state is harder to reuse and test — if the profession display ever needs to be shown again (e.g., a "change class" feature), the selection logic must be disentangled from it first.

  ```cpp
  // Game.cpp:27 — intro() prints lore, displays professions, reads input, and reinitializes hero
  void Game::intro() {
      // ... 40 lines of narrative and profession display ...
      int c; std::cin >> c;
      // selection + hero reinitialization mixed in same function
      hero = Character(p, "Dragon Warrior");
      maxHP = hero.getMaxHP();
  ```

**Operator Overloading — 7 / 10**
- `operator<<` (Character), `operator==` (Item), `operator<<` (Inventory) all fully correct
- `operator<<` (Item) prints name and value but omits the explicit type label (Weapon/Potion/Armor not printed) (−1)

  Without a type label, an item printed as "Qingfeng Sword (+8)" is indistinguishable in format from a potion — any code or player that relies on the display to identify item category cannot do so.

  ```cpp
  // Item.cpp:14 — operator<< omits type; prints only name and value
  std::ostream& operator<<(std::ostream& os, const Item& item) {
      os << item.getName() << " (+" << item.getValue() << ")";
      return os;
      // missing: type label such as "Weapon" / "Potion" / "Armor"
  }
  ```

- No bonus overloads

**Heap / Dynamic Memory — 9 / 10**
- All 6 Character instances (hero + 5 enemies) stored by value in Game; `currentEnemy` is a non-owning pointer to a value member (no `new`/`delete`); Inventory and weapon vector by value
- Minor deduction: polymorphic subclassing infrastructure in place (virtual `takeDamage`, protected members, `currentEnemy*`) but no derived classes created to demonstrate it (−1)

  If a derived class is ever deleted through a `Character*` pointer, the compiler will call only `~Character()` and skip the derived destructor entirely, silently leaking any resources the derived class owns — this is undefined behavior per the C++ standard.

  ```cpp
  // Character.hh — virtual takeDamage declared, but no virtual destructor
  class Character {
  public:
      virtual void takeDamage(int amount);  // virtual method present
      // missing: virtual ~Character() = default;
  protected:
      std::string _name;
      int _hp, _max_hp, _atk, _def;
  ```

**Inheritance — 0 / 10**
- Not used. `Character` declares `virtual void takeDamage` and exposes protected members — the full subclassing infrastructure is in place — but no derived classes were created. A `DragonKing` subclass overriding `takeDamage()` to trigger Tidal Wave/Water Shield/Dragon Roar at HP thresholds would have completed what the README describes and earned all 10 points with almost no structural changes.

## Bonus

**Bonus score: 9 / 10**

Three professions with mechanically distinct abilities, five enemies each with their own backstory, a weapon upgrade system, EXP and coin economy, a shop, and phase-based Dragon King attacks — this is a complete game in every meaningful sense. The Ming Dynasty 1428 setting with the five Great Evils gives it a real sense of a world.

The profession design is especially strong. Dodge, damage reduction, and lifesteal on crit are all meaningfully different playstyles, not just stat variations.

**Clean Code note:** The leading underscore convention is applied consistently across all classes, there's no `using namespace std` anywhere, and the 14 private Game methods give clear single-responsibility separation — `showEnemyStory()`, `showVictoryLore()`, `getCurrentWeaponDmg()`, `useSkill()`, `spendExp()`, `shop()` are all focused and readable. `run()` is just 3 lines delegating to `intro()`, `loop()`, and `checkEnding()`. The code is genuinely pleasant to read and reflects real architectural care. The one gap is that the Dragon King phase logic lives inline in `loop()` rather than in its own method — a minor thing in an otherwise exemplary submission.
