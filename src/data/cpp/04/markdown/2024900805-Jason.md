# Project Report — Jason

| | |
|---|---|
| **Student** | 2024900805 — Jason |
| **Submission** | On time |
| **Final Score** | 54 / 100 |

---

## Overall

"Dungeon of Shadows" is a minimal three-encounter dungeon crawler: the player fights Jason, MIke, and the Dragon in sequence, with a starting potion and loot drops between fights. The structure is clean — `run()` delegates to `battle()`, which delegates to focused helpers (`playerTurn()`, `enemyTurn()`, `useItem()`, `showStatus()`) — and the canonical form for `Inventory` is correctly implemented using a raw pointer-to-vector. The game compiles cleanly via cmake and plays to a win or loss. The main issue is that all four required operator overloads (`operator<<` for Character, `operator==` and `operator<<` for Item, `operator<<` for Inventory) are entirely absent from the codebase, costing the full 10 points for Section 3. The README was never written — both files are the default Gitee template — costing 8 of 10 README points.

---

## Sections

**Subject Requirements — 19 / 30**
- All five required classes implemented and functional; Game.hh clean with good helper declarations; main.cpp unmodified
- **operator<< (Character), operator== (Item), operator<< (Item), operator<< (Inventory) all missing** (−3) — none of the four required operators are declared or implemented anywhere in the codebase; Character.cpp ends after setDEF() with no stream overload; Item.cpp contains only constructor and getters
- **README is the default Gitee template** (−8) — README.md and README.en.md both contain only Gitee boilerplate with no game description, no enemies, no items, and no end-condition explanation; the student ID appears only as part of the auto-generated repo title line

**Clean Code — 28 / 40**
- `battle()` (Game.cpp:140) loops cleanly, delegating to focused helpers; `run()` (Game.cpp:163) is 10 lines and reads like a high-level script; `showStatus()`, `clearScreen()`, `waitEnter()`, `enemyTurn()`, `attemptFlee()` are all 5–10 lines and single-purpose
- **`using namespace std;` at file scope** (−2) — Game.cpp:6 pollutes the global namespace; a pattern the course teaches against
- **Magic numbers throughout** (−6) — player stats `50, 8, 2` (Game.cpp:9), enemy stats (Game.cpp:17-19), flee chance raw `7` in `(rand() % 10) < 7` (Game.cpp:51), all item heal/boost values are inline integers with no named constants; no `const int PLAYER_MAX_HP` or equivalent anywhere
- Naming is solid: `currentEncounter`, `gameOver`, `attemptFlee()`, `nextEncounter()` all descriptive; minor issues are `c` for menu choice (Game.cpp:89) and `"MIke"` typo (Game.cpp:18)

  ```cpp
  // Game.cpp:51 — flee chance as a bare magic number
  return (rand() % 10) < 7; // 70% flee chance
  // Could be: static constexpr int FLEE_THRESHOLD = 7;
  ```

**Operator Overloading — 0 / 10**
- None of the four required operators are present anywhere in the codebase; the only `operator` definition is `Inventory::operator=` (copy-assignment), which is a canonical-form requirement
- The declaration sites in Character.hh (not present — was never added) and Inventory.hh (not present) confirm they were not attempted

  These four operators are worth 8 of the 10 points in this section and appear explicitly in the rubric. Adding `operator<<` for each class requires roughly 10 lines each; it is the most direct path to recovering points in a revision.

**Heap / Dynamic Memory — 7 / 10**
- Inventory wraps a `std::vector<Item>` in a raw pointer (Inventory.hh:21: `std::vector<Item>* items`); Rule of Three is complete and correct: copy ctor deep-copies via `new std::vector<Item>(*other.items)` (Inventory.cpp:8), copy-assign has self-assignment guard (Inventory.cpp:12), destructor calls `delete items` (Inventory.cpp:19)
- No leaks, no double-free, no dangling pointers
- **Design deduction** (−3) — a pointer-to-vector is an unusual pattern; the `std::vector` already owns its elements on the heap; adding a raw pointer on top introduces risk without benefit; a direct member `std::vector<Item> items` would be cleaner

**Inheritance — 0 / 10**
- No inheritance used; `std::vector<Character> enemies` (Game.hh:25) stores all enemies by value as plain `Character` objects. Score: 0/10.

## Bonus

**Bonus score: 2 / 10**

"Dungeon of Shadows" is a working, playable game that compiles and runs to completion. The code structure shows good instincts — the helper decomposition in Game.hh is genuinely well-organized for the course level. However, the game itself has no story, no atmosphere, and no mechanics beyond a flat combat loop against three enemies named after classmates. The narrative ambition is essentially zero: enemies are called "Jason", "MIke", and "Dragon"; the player is called "Hero"; items are "Small Healing Potion" and "Iron Sword". The README was never written. What's here is a functional skeleton rather than a game.

The two points awarded reflect that the project compiles, runs to both a win and a loss condition, and has a clean enough code structure that the bones of a real game are visible.
