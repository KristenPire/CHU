# Project Report — Gillian

| | |
|---|---|
| **Student** | 2024900814 — Gillian |
| **Submission** | On time |
| **Final Score** | 94 / 100 |

---

## Overall

"Escape the Lab" is a tight three-enemy dungeon RPG where a test subject fights through a secret laboratory to defeat the AI Core boss. The code quality is about as clean as a beginner C++ project gets — k-prefix constants for every game value, trailing underscore on every private member, no using namespace std anywhere, std::out_of_range exceptions from Inventory, and static_cast throughout. The only section missing points is inheritance, where the AI Core boss would have been a natural candidate for a Boss subclass.

---

## Sections

**Subject Requirements — 30 / 30**
- All five required classes correctly implemented with unchanged constructor signatures
- Three enemies (Security Robot, Mutant Creature, AI Core boss) with a locked boss fight requiring two prior wins
- Complete README with enemy names/roles, item effects, win/lose conditions, and flee rules

**Clean Code — 39 / 40**
- Exceptional decomposition: 13 named functions all appropriately sized (setupInventory 5 lines, setupEnemies 6 lines, showStatus 4 lines, calculateDamage 3 lines, waitForEnter 1 line)
- k-prefix `constexpr` constants in anonymous namespace for all magic numbers; trailing underscore on every private member across all files; no `using namespace std` anywhere
- Anonymous namespace for all file-local helpers (printDivider, printSectionTitle, typeToString, all kConstant values); `std::max(0, ...)` guards in constructors; `std::out_of_range` from Inventory; robust cin.ignore with `std::numeric_limits`
- **battleEnemy() slightly oversized** (−1) — ~45 lines handling display, player action dispatch, enemy counter-attack, and alive-checks in a single loop body; splitting applyPlayerAction() and resolveEnemyTurn() would reduce it to ~25 lines

  A function that mixes display, input handling, and two-sided combat resolution is harder to test and harder to modify — a bug in the enemy counter-attack logic requires navigating past the player-action code to find it.

  ```cpp
  // Game.cpp:159 — battleEnemy() loop body handles all four concerns inline
  bool Game::battleEnemy(Character& enemy, bool can_flee)
  {
      while (player_.isAlive() && enemy.isAlive())
      {
          showStatus();
          const int action = promptAction(can_flee);
          if (action == kAttackAction) { /* player attacks */ }
          else if (action == kUseItemAction) { /* item use */ }
          else if (can_flee && action == kFleeAction) { return true; }
          if (!enemy.isAlive()) { return true; }
          const int damage = calculateDamage(enemy, player_);  // enemy turn mixed in
          player_.takeDamage(damage);
  ```

**Operator Overloading — 8 / 10**
- All four required operators correctly implemented with full field coverage
- operator<< (Character): "Name [HP: x/y, ATK: z, DEF: w]"; operator== (Item): compares all three fields; operator<< (Item): uses typeToString() helper from anonymous namespace; operator<< (Inventory): handles empty, prints indexed items
- No bonus overloads (−2)

  Two additional points were available for any operator that fits naturally in your code — `operator+=` for healing, `operator-=` for damage, `operator<` for comparing item values, or `operator+` for merging inventories are all common choices in an RPG.

**Heap / Dynamic Memory — 10 / 10**
- All state managed via STL; std::vector<Item> items_ in Inventory; std::vector<Character> enemies_ in Game; no raw owning pointers, no new/delete; `= default` constructor and destructor throughout

**Inheritance — 0 / 10**
- Not used. The AI Core is the natural candidate: a `Character` base with an `AiCore` subclass overriding a virtual `takeTurn(Character& target)` would let the boss use a distinct attack pattern (multi-hit, phase shift, etc.) rather than just higher stats, and `battleEnemy()` could dispatch through the base pointer without a special case.

## Bonus

**Bonus score: 7 / 10**

The "SECURITY BREACH / SUBJECT STATUS: ACTIVE" ASCII title art, the encounter tags that signal boss fights before they start, and the win-screen flavor text all show real attention to player experience — that level of polish is rare in a first C++ project.

The boss locked behind two mandatory fights with no flee option creates genuine escalating stakes. The game is tight, complete, and clearly built with care.

**Clean Code note:** This is about as clean as a beginner C++ project gets — k-prefix constants for every game value, trailing underscore on every private member, no using namespace std anywhere, std::out_of_range exceptions from Inventory, static_cast throughout. The 39/40 on clean code speaks for itself. The only place to go from here is the inheritance section (the AI Core boss is a natural candidate for a Boss subclass) and splitting battleEnemy() into separate player-action and enemy-turn functions to get that last point.
