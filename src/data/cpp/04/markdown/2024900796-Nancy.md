# Project Report — Nancy

| | |
|---|---|
| **Student** | 2024900796 — Nancy |
| **Submission** | On time |
| **Final Score** | 90 / 100 |

---

## Overall

A Snow White-themed RPG with seven distinct dwarf enemies, each with their own stats and a dramatic first-appearance announcement. The game has a genuine sense of narrative arc with a win condition that requires working through all of them. Decomposition is strong throughout — `run()` is five lines. Minor deductions for a missing `README.md` file extension, a couple of weak variable names, and no inheritance.

---

## Sections

**Subject Requirements — 28 / 30**
- All five required classes implemented correctly; item properly removed from inventory after use
- Seven enemies with unique stats, Health Potion, and clear win/lose/flee conditions
- **README not named correctly** (−1) — file is named "Readme" with no `.md` extension
- **README content incomplete** (−1) — enemies referenced generically as "seven elemental dwarves" without individual names; no items listed

**Clean Code — 34 / 40**
- Excellent decomposition: `run()` (5 lines) purely delegates; `battleLoop()` (7 lines) is a clean event loop; `enemyAction()`, `initGame()`, and `askPlayerName()` are all tight and single-purpose
- **Naming** (−2) — `op` at Game.cpp:90 for player action choice; `now` for current HP is generic; `firstShow` could be `hasAppeared`

  Single-letter and generic variable names force readers to hold context in their head while reading — when `op` appears on line 94 in a conditional, you must remember from line 90 that it means "the player's action choice", not "operator" or something else entirely.

  ```cpp
  // Game.cpp:90 — single-letter name for player action choice
  void Game::playerAction() {
      int op;
      cout << "\nPlease choose your action: ";
      cin >> op;
      if (op == 1) { ... }   // "op" carries no meaning at the point of use
  ```

- **Magic numbers** (−4) — HP bar width 10, key goal 7 appears three times, and all enemy stats are raw literals in the constructor

  Magic numbers scattered across multiple lines mean that changing the win condition from 7 keys to 6 requires finding and updating every occurrence manually — missing one creates a silent logic inconsistency that won't produce a compiler error.

  ```cpp
  // Game.cpp:21-27 — all seven enemies constructed with raw stat literals
  enemies.push_back(Character("Grumpy",  50, 10, 3));
  enemies.push_back(Character("Sleepy",  40,  8, 2));
  // ... win condition: key goal 7 hard-coded at Game.cpp:30, 68, and 145
  ```

- C-style fixed array `bool firstShow[7]` where `std::array<bool, 7>` would be more idiomatic; `using namespace std` at file scope

**Operator Overloading — 8 / 10**
- All four required operators implemented correctly; `operator<<` for Inventory produces a numbered list with empty-case handling
- No bonus overloads implemented

**Heap / Dynamic Memory — 10 / 10**
- STL only; `std::vector<Item>` for inventory and `std::vector<Character>` for enemies
- Canonical form on Inventory is complete and correct; `useItem(int)` correctly erases via `items.erase()`

**Inheritance — 0 / 10**
- Not used. The `firstShow[7]` boolean array tracking which dwarf has made its entrance announcement is a parallel structure that mirrors the `enemies` vector — a `Dwarf` subclass of `Character` with a `bool hasAppeared` member and a virtual `onFirstEncounter()` would have kept each dwarf's state with the dwarf itself, removing the need for the index-coupled array entirely.

## Bonus

**Bonus score: 10 / 10**

Seven distinct enemies with their own stat profiles, a dramatic first-appearance announcement for each one, and a win condition that requires working through all of them — your game has a real sense of scale and narrative arc. The Snow White theme gives it personality that a generic dungeon setup just doesn't have.

The `firstShow` flag system is a clever small detail. Making each dwarf's entrance feel like a moment rather than just a loop iteration takes deliberate thought.

**Clean Code note:** The decomposition is strong — `run()` is five lines, `battleLoop()` is six, and all the action helpers are focused and short. A few naming choices could be cleaner: `op` for the player's action choice and `now` for current HP are a bit generic. The C-style fixed array `bool firstShow[7]` works but `std::array<bool, 7>` would be more idiomatic modern C++. Overall this is well-structured code that's easy to read and follow.
