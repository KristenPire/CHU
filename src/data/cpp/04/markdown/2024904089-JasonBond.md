# Project Report — Jason Bond

| | |
|---|---|
| **Student** | 2024904089 — Jason Bond |
| **Submission** | On time |
| **Final Score** | 75 / 100 |

---

## Overall

A sci-fi RPG set in an AI-dominated world, where the twist that Alice is a robot pays off well for a text game. The four-stage structure with loot progression from kitchen knife to Laser and NanometerArmor shows real thematic care, and the three distinct endings give the story genuine weight. The main drag is pervasive non-standard naming and a 106-line `run()` function that tries to do everything at once.

---

## Sections

**Subject Requirements — 27 / 30**
- Four enemies, three ending paths, and a stacked inventory with weapons/potions/armor all fully implemented
- Solid game loop with hero death and two victory endings after defeating the boss on floor 4
- **README incomplete** (−3) — only `README.en.md` exists (no `README.md`); game mechanics not described; enemies listed by name only with no stats; endings named but unexplained

**Clean Code — 25 / 40**
- Helper functions (`fight()`, `Youattack()`, `Aiattack()`, `useitem()`) are appropriately sized and reasonably separated
- `using namespace std;` in all header files (Character.hh, Inventory.hh, Item.hh) pollutes every translation unit that includes them
- **`run()` at 106 lines** (−5) — mixes title display, game initialization, stage setup, battle loop, story narration, reward dispensing, and all three endings inline; should be split into at least `showTitle()`, `setupGame()`, `runStage(int)`, `postStageReward(int)`, `showEnding()`

  A 106-line `run()` that handles story, setup, four distinct combat stages, reward logic, and three different endings cannot be understood, tested, or modified without reading the entire function — every change carries the risk of breaking a different stage.

  ```cpp
  // Game.cpp:143 — run() opens; 106 lines of inline stage logic follow
  void Game::run() {
      srand(time(0));
      // ~10 lines of ASCII title art
      // story narration
      int gameround = 1;
      Me = Character("Zywoo", 30, 8, 5);
      // stage 1 setup, full combat loop, reward dispense
      // stage 2 setup, full combat loop, reward dispense
      // stage 3 setup, ...
      // stage 4 + all three endings — all inline
  ```

- **Naming** (−6) — member variables use PascalCase (HP, MaxHP, ATK, DEF, Alive); Game fields `Me`, `Ai`, `it` are too abbreviated; method names `Youattack`/`Aiattack` are non-standard; typos in user strings ("demage", "srtong", "vilid")

  PascalCase field names like `HP`, `ATK`, and `Alive` violate standard C++ naming conventions, making the code harder to read for anyone familiar with the language, and `Me`/`Ai` as class-level field names give no indication of what they represent without reading the surrounding context.

  ```cpp
  // Character.hh:29 — PascalCase private fields
  std::string Name;
  int HP;
  int MaxHP;
  int ATK;
  int DEF;
  bool Alive;
  // Game.hh — abbreviated Game fields
  Character Me;   // the player
  Character Ai;   // the enemy
  Item it;        // current item
  ```

- **Note** — `bool Alive` is a redundant field: `takeDamage()` sets `Alive=false` without zeroing `HP`, so `getHP()` can return positive while `isAlive()` returns false; `choose()` calls itself recursively for invalid input (stack overflow risk)

  When `Alive` and `HP` are separate truth sources for the same concept, they can desynchronize — `takeDamage()` sets `Alive=false` but leaves `HP` unchanged, so `getHP()` returns a positive number while `isAlive()` returns false, creating contradictory state that will produce wrong behavior in any code that checks either field.

  ```cpp
  // Character.cpp:29-34 — Alive set false but HP not zeroed
  void Character::takeDamage(int amount) {
      if (amount >= 0 && HP > amount) {
          HP -= amount;
      } else if (amount >= 0 && HP <= amount) {
          Alive = false;   // HP still holds its pre-damage value
      }
  }
  // Game.cpp:133 — choose() recurses on invalid input (stack overflow risk)
  } else {
      cout << "Plesae enter vilid choice:" << endl;
      choose();   // unbounded recursion
  }
  ```

**Operator Overloading — 7 / 10**
- `operator<<` (Character) and `operator==` (Item) both correct and complete
- `operator<<` (Inventory) delegates to `displayBag()` with empty-case handling — full marks
- **`operator<<` (Item)** (−1) — prints name and value only; no type label, and missing separator between "Name" and value

  Without a separator between "Name" and the value, `operator<<` produces output like `"Name knifeValue 2"` — the fields run together with no space or delimiter, making the output unreadable and useless for any display purpose.

  ```cpp
  // Item.cpp:21 — no type label; "Name" and value run together
  std::ostream& operator<<(std::ostream& os, const Item& item) {
      os << "Name " << item.getName() << "Value " << item.getValue();
      return os;
  }
  ```

**Heap / Dynamic Memory — 10 / 10**
- All state held by value: `Character Me`, `Character Ai`, `Inventory Bag`; Inventory uses `std::vector<BagItem>` (STL-managed). No raw pointers anywhere. Full marks.

**Inheritance — 0 / 10**
- Not used. `Me` and `Ai` are both plain `Character` instances, but their turn logic diverges significantly — player input in `Youattack()`/`choose()` vs. fixed AI in `Aiattack()`. A `Character` base with `Hero` and `AIEnemy` subclasses, each overriding a virtual `takeTurn()`, would have let `run()` call `Me.takeTurn()` and `Ai.takeTurn()` in a clean loop instead of calling different free functions for each side.

## Bonus

**Bonus score: 6 / 10**

The sci-fi premise is genuinely fun, and the twist — that Alice was a robot all along — lands well for a text RPG. That kind of narrative payoff takes real care to set up.

The three endings give the story actual weight, and the loot progression from kitchen knife to Laser and NanometerArmor tracks the world beautifully. A lot of games skip that kind of thematic consistency.

**Clean Code note:** The codebase is compact and self-contained, and the battle loop structure in `run()` is easy enough to follow once you understand the four-stage progression. Helper functions like `fight()`, `Youattack()`, `Aiattack()`, and `useitem()` do keep responsibilities reasonably separated. The main improvement opportunity is splitting `run()` itself — at 106 lines it handles story setup, stage transitions, combat loop, reward dispensing, and all three endings inline. Naming conventions also need work (uppercase fields like `HP`, `ATK`, and method names like `Youattack`/`Aiattack` are non-standard C++), but the underlying logic is readable and honest.
