# Project Report — Melkiades

| | |
|---|---|
| **Student** | 2024900804 — Melkiades |
| **Submission** | On time |
| **Final Score** | 90 / 100 |

---

## Overall

"Escape from the Dark Cellar" is a focused two-phase game: an exploration/preparation phase where you manage inventory and rest, followed by a boss battle against the Skeleton King. The pacing is deliberate — the player chooses when they're ready to fight. The code is among the cleanest in the cohort, with all four required operators correct, consistent trailing underscore convention throughout, and a 11-line `run()`. The main weaknesses are a missing flee option (only 2 endings), a filename typo that would break Linux builds, and no inheritance.

---

## Sections

**Subject Requirements — 27 / 30**
- All five required classes fully implemented; correct interfaces with trailing underscore convention; two-phase game structure well-executed; all mechanics present
- **End conditions** (−1) — Only 2 distinct endings (win/lose); no flee/escape option
- **README** (−2) — File is `README.en.md` instead of `README.md` (−1); enemy stats not listed (Skeleton King HP/ATK/DEF absent from README) (−1)

**Clean Code — 35 / 40**
- Excellent small functions: `run()` (11 lines), `randomInRange()` (3 lines), `showGameOver()` (12 lines), `showPreparationMenu()` (~35 lines clean switch)
- Trailing underscore convention on all private members across all classes; no `using namespace std` anywhere; `mt19937` with chrono seed; `cin.clear/ignore` input validation
- **`doBattle()` too large** (−3) — ~90 lines handling player attack, item selection with inventory display and validation, boss attack, and win/lose checks all inline; extracting `handlePlayerAttack()`, `handlePlayerItemUse()`, `handleBossAttack()` would reduce it to a clean ~20-line coordination loop

  A 90-line battle function that contains item display, input validation, healing logic, boss attack, and win/lose checks all inline is four different responsibilities in one place — changing how items are validated requires reading past all the attack and boss code to find the right section.

  ```cpp
  // Game.cpp:67-155 — doBattle() handles all phases of combat in one ~90-line function
  void Game::doBattle() {
      bool battleEnd = false;
      while (!battleEnd) {
          // player attack (lines 86-97)
          // item use: display inventory + validate + apply (lines 99-126)
          // boss attack (lines 141-154)
          // win/lose checks interspersed throughout
      }
  }
  ```

**Operator Overloading — 8 / 10**
- All 4 required operators fully correct: `operator<<` (Character) shows all fields; `operator==` (Item) compares all three fields; `operator<<` (Item) shows name, type label, and value; `operator<<` (Inventory) handles empty case
- No bonus overloads

**Heap / Dynamic Memory — 9 / 10**
- All state by value — Character player, boss, Inventory, mt19937 — no dynamic resources anywhere
- Minor deduction: no polymorphic heap management demonstrated (both characters stored as value members) (−1)

  Storing both characters as plain value members rather than using any pointer-based ownership means the game never demonstrates heap allocation or deallocation — valid and safe, but it sidesteps the memory management concepts the assignment is designed to test.

  ```cpp
  // Game.hh — player and boss stored as Character values, not pointers
  Character player;   // by value; no new/delete anywhere
  Character boss;
  // No Character*, no unique_ptr<Character>, no dynamic allocation demonstrated
  ```

**Inheritance — 0 / 10**
- Not used. `player` and `boss` are both plain `Character` values with identical interfaces but completely different turn logic hardcoded across `doBattle()` — a `Combatant` base with `Player` and `SkeletonKing` subclasses, each overriding a virtual `takeTurn()`, would let `doBattle()` loop over two `Combatant*` pointers instead of branching on which side is acting.

## Bonus

**Bonus score: 11 / 10**

The two-phase structure works really well — check your status, manage inventory, rest if you need to, and when you're ready, push open the door and face the Skeleton King. That pacing gives the player real agency instead of just throwing them into a fight.

The named Good/Bad endings and the Chinese UI give it a lot of atmosphere. Starting with two potions and the option to rest means the game is actually designed to be winnable, which is more than a few submissions can say.

**Clean Code note:**
This is some of the cleanest code in the cohort — the trailing underscore convention is applied consistently on every private member across all classes, `run()` is just 11 lines, `randomInRange()` is a clean 3-line helper using `mt19937`, and input validation with `cin.clear/ignore` is handled properly. All four required operators are fully correct. The only real blemish is the `Itim.hh` filename typo and the `Main.cpp` capitalization mismatch, both of which would break a Linux build — small oversights in an otherwise very tidy submission.
