# Project Report — Makayla

| | |
|---|---|
| **Student** | 2024900810 — Makayla |
| **Submission** | On time |
| **Final Score** | 93 / 100 |

---

## Overall

A scuba diver RPG inspired by Dave the Diver, with weapon choice between Harpoon and Speargun (genuinely different damage profiles), route selection that changes both the enemy and the loot, a shark finale with a one-time evasion move, and handcrafted ASCII win/game over art. The code quality is consistently high — trailing-underscore naming, precise action-verb function names, and `std::mt19937` for randomness. No inheritance used.

---

## Sections

**Subject Requirements — 30 / 30**
- All five required classes implemented correctly with full operator support
- Three sea creature enemies with distinct behaviors; supply box items (Oxygen Tank, Suit, Upgrade Kit) stored and usable
- Perfect README: enemy stats, item types, win/lose conditions, and a story with personal motivation all documented

**Clean Code — 37 / 40**
- Exceptional naming: trailing-underscore convention on every member, all functions are precise action verbs (`chooseOption`, `describeItemEffect`, `giveFishBonus`, `openSupplyBox`), no single-letter variables
- Short focused functions throughout: `chooseWeapon()` (4 lines), `getPlayerDamage()` (4 lines), `showStatus()` (7 lines), `describeItemEffect()` (9 lines)
- Uses `std::mt19937` with proper seeding instead of `rand()` — notably professional
- **Function size** (−2) — `fight()` (~50 lines) embeds a multi-line shark-flee narrative block that could be its own helper; `useItem()` (32 lines) is borderline

  A 50-line `fight()` that inlines the shark-flee narrative (a multi-step story branch that adds items and prints several lines of text) makes the core combat loop harder to read — the shark-specific narrative has nothing to do with the general damage calculation and enemy-turn logic surrounding it.

  ```cpp
  // Game.cpp:109-119 — multi-line shark flee narrative embedded inside fight() loop
  if (enemy.getName() == "Great White Shark" && !sharkSupplyFound_) {
      sharkSupplyFound_ = true;
      inventory_.addItem(Item("Emergency Oxygen Tank", Item::Type::Potion, 8));
      std::cout << "You dive away from the Great White Shark...\n";
      std::cout << "Behind the rocks, you discover a dark hidden passage...\n";
      std::cout << "Inside the broken cabin, you find an Emergency Oxygen Tank...\n";
      std::cout << "With one more chance to survive, you return to face the shark.\n";
      continue;
  }
  ```

- **Code duplication** (−1) — item-effect branching logic appears nearly identically in both `useItem()` and `giveFishBonus()`; an `applyItemEffect()` helper would eliminate it

  Duplicated item-effect logic means that adding a new item type (or changing how Armor items work) requires updating two separate functions in sync — missing one creates a silent inconsistency where the item behaves differently depending on whether it came from `useItem()` or `giveFishBonus()`.

  ```cpp
  // Game.cpp:71-84 — useItem() item-effect branch
  if (item.getType() == Item::Type::Potion) { player_.heal(item.getValue()); }
  else if (item.getType() == Item::Type::Armor) { player_.addDEF(item.getValue()); }
  else { player_.addATK(item.getValue()); }

  // Game.cpp:150-158 — giveFishBonus() near-identical branch
  if (reward.getType() == Item::Type::Potion) { player_.heal(reward.getValue()); }
  else if (reward.getType() == Item::Type::Armor) { player_.addDEF(reward.getValue()); }
  else { player_.addATK(reward.getValue()); }
  ```

**Operator Overloading — 8 / 10**
- All four required operators correctly implemented
- No bonus overloads implemented

**Heap / Dynamic Memory — 10 / 10**
- STL only; `std::vector<Item>` used throughout with no raw `new`/`delete`
- Canonical form on Inventory is complete and correct

**Inheritance — 0 / 10**
- Not used. Jellyfish, Moray Eel, and Great White Shark are all plain `Character` objects differentiated only by name-string checks in `fight()` — a `SeaCreature` base with a virtual `attackBehavior()` would replace the `if (enemy.getName() == "Great White Shark")` branch with a proper polymorphic dispatch and make adding future enemies trivial.

## Bonus

**Bonus score: 8 / 10**

A scuba diver fighting sea creatures, weapon choice between a Harpoon and Speargun with genuinely different damage profiles, route selection that changes your enemy and your loot, and a shark finale with a one-time evasion move — this is one of the most original concepts in the whole batch. The Dave the Diver inspiration is clear and it works really well.

The fish-as-loot system (different fish drop different bonuses depending on your route) is creative and thematic. And the handcrafted ASCII WIN and GAME OVER art are a great finishing touch.

**Clean Code note:** The code quality here is genuinely strong — consistent trailing-underscore naming on every member, all function names are precise action verbs, and the reusable `chooseOption()` validator is used everywhere. `describeItemEffect()` as a const method returning a string is a clean design. The only real duplication is the item-effect branching logic that appears nearly identically in `useItem()` and `giveFishBonus()` — extracting an `applyItemEffect()` helper would eliminate that. Otherwise this is very readable, well-structured code.
