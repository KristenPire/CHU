# Project Report — Otherson

| | |
|---|---|
| **Student** | 2024902584 — Otherson |
| **Submission** | On time |
| **Final Score** | 93 / 100 |

---

## Overall

A three-hero party RPG where Warrior, Mage, and Priest each have their own job class, MP pool, and skill options — and each can die independently across three escalating waves. The mana enforcement and level-up system give the combat real texture. The main area to improve is `battleLoop()`, which accumulates four levels of nesting and handles too many concerns in one place.

---

## Sections

**Subject Requirements — 29 / 30**
- All five required classes implemented with required interfaces intact; four monster types across three waves
- Mana Potion added as an extra item type; all four items usable in combat
- **README incomplete** (−1) — enemies referenced only as "monsters" with no names listed; items entirely absent from README

**Clean Code — 31 / 40**
- `run()` delegates cleanly to `initGame()`, `startMainLoop()`, and `battleLoop()`; `generateMonsters()` is short and focused
- Consistent underscore-prefix convention (`_name`, `_hp`, `_items`, `_wave`) across all classes
- Canonical form on Inventory is complete and correct; deep copy via `new std::vector<Item>` is properly paired with `delete`
- **Function size** (−4) — `battleLoop()` is 122 lines with four levels of nesting (for-hero → while-action → while-skill → while-target); skill selection and target selection loops could each be extracted into their own helpers

  Four levels of nested loops in a 122-line function mean that adding a new skill type or changing target selection logic requires navigating to the innermost loop while keeping the outer state in mind — the cognitive load of reading and modifying this function grows with every level of nesting.

  ```cpp
  // game.cpp:103 — battleLoop() with four nesting levels
  void Game::battleLoop() {
      for (int i = 0; i < _heroes.size(); i++) {        // level 1: per hero
          while (true) {                                  // level 2: action choice
              if (choice == 1) {
                  while (true) {                          // level 3: skill choice
                      if (skillChoice >= 1 && ...) {
                          while (true) {                  // level 4: target choice
                              cin >> targetChoice;
  ```

- **Magic numbers** (−4) — hero HP/ATK/DEF literals in `initGame()`, exp threshold 50, level-up increments 20/10/5/2 — none are named constants; platform-specific `system("read -p 'Press Enter...'")` will break on macOS/Windows; C-style casts `((Monster&)_monsters[i])` should be `static_cast`

  Magic numbers scattered across `initGame()` and `levelUp()` mean that changing the Warrior's starting HP from 100 to 120 requires finding the right literal among several unrelated hero stats — a named constant like `kWarriorHP` would make the change immediate and self-documenting.

  ```cpp
  // game.cpp:18-20 — hero stats as bare literals with no named constants
  _heroes.push_back(Character("Warrior", 100, 25, 8, Character::Job::WARRIOR));
  _heroes.push_back(Character("Mage",     60, 30, 3, Character::Job::MAGE));
  _heroes.push_back(Character("Priest",   80, 15, 5, Character::Job::PRIEST));
  // game.cpp:253-257 — level-up increments also as bare literals
  int hpIncrease = 20; int mpIncrease = 10; int atkIncrease = 5; int defIncrease = 2;
  ```

**Operator Overloading — 8 / 10**
- All four required operators implemented correctly
- `operator==` on Item compares `_name`, `_type`, `_value`; `operator<<` on Inventory gives a numbered list or "Empty Inventory"
- No bonus overloads (−2)

  Two additional points were available for any operator that fits naturally in your code — `operator+=` for healing, `operator-=` for damage, `operator<` for comparing item values, or `operator+` for merging inventories are all common choices in an RPG.

**Heap / Dynamic Memory — 10 / 10**
- Raw pointer mode: Inventory wraps its vector in a heap-allocated pointer; every `new` has a matching `delete`
- Self-assignment guard prevents double-free; no dangling pointers detected
- Note: wrapping a vector in a raw pointer is unnecessary indirection, but the memory management is functionally correct

**Inheritance — 7 / 10**
- Monster inherits from Character — a genuine is-a relationship; Monster adds `getMonsterType()` and `getExpReward()` as appropriate extensions
- **No virtual destructor** (−2) — Character.hh has no virtual destructor; technically UB if deleted via Character pointer

  If any code ever holds a Monster through a `Character*` and deletes it — a natural thing to do when the hierarchy grows — the compiler invokes only `~Character()`, skipping `~Monster()` and any cleanup it might perform; this is undefined behavior even if Monster currently owns no resources, because the standard makes no guarantee about what happens.

  ```cpp
  // Character.hh:6-43 — no virtual destructor despite Monster inheriting from Character
  class Character
  {
  public:
      Character(const std::string& name, int hp, int atk, int def, Job job);
      // ... getters, takeDamage, heal ...
      // missing: virtual ~Character() = default;
  private:
      std::string _name; int _hp; int _maxHP; int _atk; int _def;
  ```

- No virtual methods means hierarchy is pure code reuse rather than polymorphism (−1)

## Bonus

**Bonus score: 8 / 10**

A three-hero party where Warrior, Mage, and Priest each have their own job class, MP pool, and skill options — and each can die independently — is genuinely ambitious. The mana cost enforcement and the level-up system that triggers on kills give the combat real texture, and the four monster types across three escalating waves give the game a satisfying arc.

The "Congudulations!" is charming. That kind of thing makes a game feel like it was made by a real person, and it was.

**Clean Code note:** The overall structure is solid — `run()` delegates cleanly to `initGame()`, `startMainLoop()`, and `battleLoop()`, and `generateMonsters()` is short and focused. The strongest area for improvement is `battleLoop()`, which has four levels of nesting and handles hero-turn dispatch, skill selection, target selection, and enemy attacks all in one 122-line function — breaking the skill and target selection into their own helpers would make it much more readable. Magic numbers for hero stats and level-up thresholds would also benefit from named constants.
