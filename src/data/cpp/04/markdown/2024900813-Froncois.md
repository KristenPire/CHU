# Project Report — Froncois

| | |
|---|---|
| **Student** | 2024900813 — Froncois |
| **Submission** | On time |
| **Final Score** | 97 / 100 |

---

## Overall

A graduation trial RPG where you fight mirror versions of the exam committee, and your final score is calculated from how many rounds you took and how many potions you burned — a self-aware meta-joke that fits the assignment perfectly. The AP combat system with burn, focus buffs, and burst abilities gives the three-floor structure real strategic depth. Clean Code is perfect; the only missing piece is an inheritance hierarchy.

---

## Sections

**Subject Requirements — 30 / 30**
- All required classes fully implemented with correct interfaces; all four operators present
- `run()` at 25 lines reads as a clean game script; `main.cpp` unmodified (79 bytes, exactly skeleton)
- Full enemy stat table and item list in README; failure and success endings explicitly described

**Clean Code — 40 / 40**
- Exceptional decomposition: no function exceeds 30 lines; `processPlayerTurn()` is 7 lines, `fightEnemy()` is 19 lines, `processPlayerAction()` is 30 lines
- Every magic number replaced with a named `kConstant` in an anonymous namespace; trailing underscores applied uniformly
- `CombatSystem` and `GameUI` as namespaces keep game logic, combat math, and UI display genuinely separated
- `BattleState` struct cleanly captures per-fight ephemeral state (AP, burn, focus buff) without polluting the class
- Full marks — textbook clean code at this level

**Operator Overloading — 8 / 10**
- All four required operators correctly implemented; `operator<<` on Item is type-aware (Weapon/Potion/Armor/Focus all handled)
- No bonus operator overloads (−2)

**Heap / Dynamic Memory — 10 / 10**
- STL only — no raw pointers anywhere. Inventory uses `std::vector<Item>` with correct canonical form. All objects passed by reference where mutation is needed. Full 10/10.

**Inheritance — 0 / 10**
- Not used. `CombatSystem` and `GameUI` are namespaces, not classes. The three floor enemies (stored as `EnemyData` structs) are all plain `Character` objects with no polymorphic behavior — an `Enemy` class inheriting from `Character`, each floor's opponent overriding a virtual `onTurnStart(BattleState&)` to apply burn or focus debuffs, would let `fightEnemy()` dispatch per-enemy specials through a single interface.

  The assignment requires a class hierarchy with at least two levels (a base class and at least one derived class); namespaces cannot be subclassed, so no is-a relationship exists anywhere in the codebase.

  ```cpp
  // Game.hh — all four major types are independent classes with no base
  class Character { ... };   // no base class
  class Item      { ... };   // no base class
  class Inventory { ... };   // no base class
  class Game      { ... };   // no base class

  // CombatSystem and GameUI are namespaces, not classes
  namespace CombatSystem { ... }
  namespace GameUI       { ... }
  ```

## Bonus

**Bonus score: 9 / 10**

The game concept is genuinely clever — a graduation trial where you fight mirror versions of the exam committee, and your final score is calculated based on how many rounds you took and how many potions you burned. That's a funny and self-aware meta-joke that actually fits the assignment perfectly.

The AP combat with burn, focus buffs, and burst abilities gives the three-floor structure real strategic depth. Combined with three meaningfully different class options, there are actual decisions to make throughout the run.

**Clean Code note:** This is close to textbook clean code at this level. Every constant is named with the k-prefix convention, every magic number is gone, trailing underscores are applied uniformly, and the GameUI/CombatSystem namespace separation keeps concerns genuinely isolated. The BattleState struct cleanly captures per-fight ephemeral state without polluting the class. If anything, the only room for improvement would be splitting the grading system logic out of calculateGrade() into a small data table rather than an if-chain, but that's a minor point on an otherwise exemplary codebase.
