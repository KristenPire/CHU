# Project Report — Verena

| | |
|---|---|
| **Student** | 2023905495 — Verena |
| **Submission** | On time |
| **Final Score** | 100 / 100 |

---

## Overall

A tight, well-paced dungeon crawl called Echo Crypt: Mira fights through three named locations (Bone Guard, Well Spirit, Crypt Beast), each dropping an item that feeds directly into the next fight. Every design choice feels intentional — including a flee option with its own ending ("the curse still whispers"). The code is among the cleanest in the batch, with anonymous namespace UI helpers, TurnResult and Encounter structs that bundle related data naturally, and a 15-line `run()` that delegates entirely to `fightEncounter()`. No inheritance was used, which costs 10 points.

---

## Sections

**Subject Requirements — 30 / 30**
- All five required classes fully implemented; English README with named enemies, items with effects, and all three end conditions (win/lose/flee) explicitly stated
- Three distinct endings: victory, death, and escape — flee is a valid outcome with its own story text
- Small Potion (heal), Iron Dagger (ATK+2), Guard Cloak (DEF+1) — all usable and all matter in the fight that follows

**Clean Code — 35 / 40**
- `run()` (15 lines) delegates entirely to `fightEncounter()`; `playerTurn()` (18 lines), `enemyTurn()` (8 lines), `askChoice()` (16 lines) — all focused
- Anonymous namespace UI helpers (`makeBar`, `fitLine`, `printBoxLine`, `printWrappedBoxText`) — excellent practice; no `using namespace std`, no `exit(0)`
- Encounter struct bundles enemy and reward by value; TurnResult struct bundles messages and fled flag — clean data design
- **`useItem()` mixes concerns** (−2) — 40 lines combining item-selection display and item-application logic; application could be a separate `applyItem()` helper

  Combining display and application logic in a single function means neither part can be reused independently — if you ever want to apply an item effect from code other than the player's inventory menu, you have to duplicate the application logic.

  ```cpp
  // Game.cpp:308 — useItem() handles both the menu display and the effect
  Game::TurnResult Game::useItem()
  {
      if (inventory_.isEmpty()) {
          return {{"Your bag is empty."}, false};
      }
      std::cout << "\nChoose an item:\n";
      for (int i = 0; i < inventory_.size(); ++i) {
          const Item& item = inventory_.getItem(i);
          std::cout << '[' << i + 1 << "] " << item.getName() << " (";
          // ... display logic, then inline application for each type ...
  ```

- **`itemTypeName()` duplicates logic** (−2) — anonymous namespace helper duplicates the switch already present in `Item::operator<<`; redundant

  Duplicating a switch over `Item::Type` means any new item type added to the enum must be handled in two separate places — the item's own operator and the standalone helper — making omissions easy.

  ```cpp
  // Game.cpp:34 — itemTypeName() switch duplicates Item::operator<<'s switch
  std::string itemTypeName(Item::Type type)
  {
      switch (type) {
      case Item::Type::Weapon: return "Weapon";
      case Item::Type::Potion: return "Potion";
      case Item::Type::Armor:  return "Armor";
      }
      return "Item";
  }
  // Item.cpp:30 — operator<< has the same switch independently
  ```

- **Misleading `actualDamage` variable** — `enemyTurn()` computes post-DEF value for the message but passes pre-DEF `damage` to `takeDamage()` (which applies DEF again internally); result is correct but the local variable is confusing

**Operator Overloading — 8 / 10**
- All four required operators fully implemented; `operator<<` on Inventory uses Item's `operator<<` with numbered list and empty case — complete
- `operator<<` on Item includes type string via switch and value — comprehensive
- No bonus overloads (−2)

  Two additional points were available for any operator that fits naturally in your code — `operator+=` for healing, `operator-=` for damage, `operator<` for comparing item values, or `operator+` for merging inventories are all common choices in an RPG.

**Heap / Dynamic Memory — 10 / 10**
- STL-only mode; no raw pointers anywhere. All classes own members by value. Game's `vector<Encounter>` holds Encounter structs by value; Encounter stores Character and Item by value. No new/delete anywhere.

**Inheritance — 0 / 10**
- Not used. Bone Guard, Well Spirit, and Crypt Beast are plain `Character` objects stored by value inside `Encounter` structs. An `Enemy` base with a virtual `getReward()` method — overridden per subclass to return the Iron Dagger, Guard Cloak, or Great Elixir — would have made the drop logic part of each enemy's own type rather than wired into the `Encounter` struct externally.

## Bonus

**Bonus score: 17 / 10**

Three named locations with individual intro text, enemies that each drop a useful item feeding directly into the next fight, a flee option with its own ending ("the curse still whispers") — Echo Crypt does a lot with a small scope. Every design choice feels intentional.

The progression arc is tight: Iron Dagger, Guard Cloak, Great Elixir each matter in the fight that follows. It's a well-paced three-act structure that most students don't manage to land this cleanly.

**Clean Code note:** The code is among the cleanest in the batch — anonymous namespace UI helpers, TurnResult and Encounter structs that bundle related data naturally, a focused run() that delegates entirely to fightEncounter(), and consistent trailing-underscore member naming throughout. The printWrappedBoxText word-wrapping helper is a genuinely useful piece of polish. The only real rough edge is the itemTypeName() helper duplicating logic already in Item's operator<<, and the slightly misleading actualDamage variable in enemyTurn — both small.
