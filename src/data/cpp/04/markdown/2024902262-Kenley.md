# Project Report — Kenley

| | |
|---|---|
| **Student** | 2024902262 — Kenley |
| **Submission** | On time |
| **Final Score** | 91 / 100 |

---

## Overall

"VOID ABYSS" is a well-architected rogue-lite with 10 scaled floors, randomly generated Combat/Event/Rest rooms, six distinct event types, stat-scaled enemies, and a proper boss fight. The function decomposition is genuinely good — enterRandomRoom() dispatches cleanly to handlers, each combat action is its own small function, and the event handlers are all short and focused. The game is scored 0 in Inheritance, which is where the main points were left on the table.

---

## Sections

**Subject Requirements — 29 / 30**
- All five required classes fully implemented; level/exp system; proper boss room separate from regular combat
- End conditions explicit: death → "GAME OVER", floor 10 boss defeated → "CONGRATULATIONS!"
- **README enemies not enumerated** (−1) — only "randomly generated Shadow enemy" and "final Boss" mentioned; no enemy list with names and stats

  A README that omits the enemy list leaves players without the game information they need before starting — knowing enemy names and stat ranges is part of the game's presentation, not just documentation.

  ```
  // README.md — enemies section only says "randomly generated Shadow enemy" and "final Boss"
  // Expected: Shadow Soul (HP scaled, ATK scaled), ELITE Shadow (HP scaled, ATK scaled),
  //           The Abyssal Lord (HP 300, ATK 35) — no enumerated list with stats provided
  ```

**Clean Code — 34 / 40**
- Excellent decomposition: run() (18 lines), enterRandomRoom() (~18 lines), handleCombat() (~25 lines), combatAttack() (9 lines), combatFlee() (5 lines), handleRest() (8 lines), generateEnemy() (7 lines)
- Canonical form correct; all state by value — no raw pointers; no `using namespace std`
- Operator-= and operator+= used expressively throughout RoomManager (`player -= 5`, `enemy -= player.getATK()`)
- **combatBag() mixes UI and combat** (−2) — display, indexed input, item-use dispatch, and enemy counter-strike all inline; a showBagMenu() helper would separate concerns; handleBossRoom() duplicates the combat loop instead of sharing it

  When the same concern appears in two separate functions, a change to the bag menu or the enemy counter-strike logic must be applied in both places — missing one causes an inconsistency.

  ```cpp
  // RoomManager.cpp:96 — combatBag() handles display, input, item use, and counter-attack in one function
  void RoomManager::combatBag(Character& player, Inventory& inventory, Character& enemy) {
      std::cout << "\n" << inventory;
      int itemIndex = GameIO::getInt("Enter item number to use (or enter 0 to Cancel): ");
      // ... item use dispatch ...
      if (enemy.isAlive()) {
          std::cout << "🩸 The " << enemy.getName() << " strikes while you are distracted!" << std::endl;
          player.takeDamage(enemy.getATK());  // counter-strike mixed in with UI logic
      }
  ```

- **Leading underscore and emoji in source** (−2) — leading underscore (_name, _hp etc.) is a consistent convention but not trailing underscore; extensive emoji in source strings and informal comment about bonus points

  Leading underscores on names in the global namespace are reserved by the C++ standard, and while member names avoid that specific hazard, the trailing underscore idiom (`name_`) is the conventional C++ style for private members that avoids the ambiguity entirely.

  ```cpp
  // Character.hh:38 — leading underscore on all private members
  private:
      std::string _name;
      int _hp;
      int _maxHp;
      int _atk;
      int _def;
      int _level;
      int _exp;
  ```

**Operator Overloading — 10 / 10**
- All four required operators fully implemented and correct
- Bonus: `Character::operator-=(int)` and `operator+=(int)` — both actively used for expressive combat (`player -= 5`, `enemy -= player.getATK()`)

**Heap / Dynamic Memory — 10 / 10**
- All state via STL; Character, Inventory, RoomManager all by value; no raw owning pointers

**Inheritance — 0 / 10**
- Not used. Shadow Soul, ELITE Shadow, and The Abyssal Lord are all plain `Character` objects constructed in `generateEnemy()` and `handleBossRoom()`. An `Enemy` base with a virtual `takeTurn(Character&)` — overridden by `ShadowEnemy`, `EliteShadow`, and `AbyssalLord` — would have removed the separate boss combat loop in `handleBossRoom()` and let both regular and boss fights share a single dispatch path.

## Bonus

**Bonus score: 8 / 10**

The pop quiz event where "Kristin blocks your path" with a C++ question is the best Easter egg in the batch — that level of personal humor makes it clear you actually enjoyed making this game.

The rogue-lite design is solid too: 10 floors of randomly generated Combat/Event/Rest rooms, stat-scaled enemies, a proper boss at floor 10, and six distinct event types that keep each run varied. Using `player -= 5` and `enemy -= player.getATK()` throughout RoomManager is exactly the kind of expressive operator usage that makes bonus overloads worth doing.

**Clean Code note:** The decomposition is genuinely good — enterRandomRoom() dispatches cleanly to handlers, each combat action (attack, flee, bag) is its own small function, and the event handlers are all short and focused. The leading underscore convention (_name, _hp, etc.) is consistent throughout, which matters more than which specific convention is used. The emoji in source strings and the informal motivational comment about bonus points are charming rather than problematic — just something to consider in production code where the audience might be broader than a game jam.
