# Project Report — Deckard

| | |
|---|---|
| **Student** | 2024903709 — Deckard |
| **Submission** | On time |
| **Final Score** | 100 / 100 |

---

## Overall

A Slay-the-Spire-inspired dungeon climber with four heroes differentiated by unique relic passives — a clean design choice that achieves meaningful class variety without separate skill trees. The armor-as-damage-buffer mechanic and enemy skill system (probabilistic defend/skill/attack) make combat less predictable than the typical random-damage loop. The main issues are a dead `Hero::_name` member that silently shadows `Character::_name` and a bug in the `getInt()` input helper.

---

## Sections

**Subject Requirements — 29 / 30**
- All required classes fully implemented with correct interfaces; all four operators present
- Five enemies across three floors with a named boss; Bad End / Good End clearly described in README
- **CMakeLists.txt not at repo root** (−1) — located at `project/CMakeLists.txt`; `cmake` from root fails

**Clean Code — 34 / 40**
- Good decomposition: `run()` (24 lines), `battleLoop()` (29 lines), `enemiesAction()` (18 lines), `playerAttack()` (19 lines), `useRelic()` (14 lines) — all focused and appropriately sized
- 8 files with clear responsibilities; armor-buffer system and Enemy skill system cleanly designed
- **heroSelection() too large** (−2) — 43 lines mixing UI rendering of all 4 hero descriptions, the input loop, and hero construction inline; separating `showHeroMenu()` from `selectHero()` would help

  Mixing display, input, and object construction in one function means you cannot change the menu layout without touching the selection logic, and the function's intent — pick a hero — is buried under four hero descriptions.

  ```cpp
  // Game.cpp:136 — hero display, input loop, and construction all in one block
  void Game::heroSelection(){
      // ... prints all 4 hero descriptions inline ...
      while (true){
          int choice = getInt("Please enter index of hero you want to choose: ");
          if (choice < 0 || choice >= 4){ /* ... */ }
          // ... constructs hero inline ...
      }
  }
  ```

- **getInt() bug** (−2) — `if (!cin >> choice)` at line 28 is `(!cin) >> choice` due to operator precedence — the error-recovery branch is unreachable; should be `if (!(cin >> choice))`

  Because `!` binds tighter than `>>`, the error branch never executes: any invalid input silently falls through and `choice` holds whatever value it had before, leading to unpredictable game behavior instead of a retry prompt.

  ```cpp
  // Game.cpp:23 — bug: (!cin) >> choice, not !(cin >> choice)
  int Game::getInt(string message){
      while (true){
          int choice = 0;
          cout << "\n" << message;
          cin >> choice;
          if (!cin >> choice) {   // operator precedence bug — unreachable branch
              cin.clear();
              cin.ignore(10000, '\n');
  ```

- **Dead Hero::_name member** (−2) — `string _name` in Hero silently shadows `Character::_name`, is never set or read, and creates silent dead data

  When a derived class declares a member with the same name as a base class member, the derived copy is default-initialized and independent; any code reading `_name` on a Hero pointer gets different data depending on whether it goes through Character or Hero, making the object internally inconsistent.

  ```cpp
  // Hero.hh:13 — _name declared in Hero despite Character already owning _name
  class Hero : public Character {
  public:
      Hero(const std::string& name, int hp, int atk, int def);
      // ...
  private:
      std::string _name;  // shadows Character::_name; never set or read
      int _exp;
  };
  ```

- **using namespace std in all .cpp files** (−2) — consistent style concern throughout

  `using namespace std` at file scope pulls the entire standard library into the global namespace, which can silently cause name collisions and makes it harder to tell at a glance whether a name comes from the standard library or the project.

  ```cpp
  // Hero.cpp:3, Game.cpp:3, Character.cpp:... — repeated in all source files
  using namespace std;
  ```

**Operator Overloading — 8 / 10**
- All four required operators correctly implemented; `operator<<` on Inventory handles empty case and uses Item's `operator<<`
- No bonus operator overloads (−2)

**Heap / Dynamic Memory — 10 / 10**
- All data owned by value: `Hero _hero`, `Inventory _backpacks`, `Item _weapon/_armor/_relic`, `vector<Location> _locations`, `vector<Enemy> _enemies` — all stored in Game by value. No raw owning pointers, no `new`/`delete` anywhere. Full 10/10.

**Inheritance — 9 / 10**
- `Character → Hero` and `Character → Enemy` are genuine is-a relationships; virtual `onDeath()` provides polymorphic dispatch; virtual destructor, `override` keyword (`Hero::onDeath() override`, `Enemy::onDeath() override`), no slicing — all technically correct
- **Hero::_name shadows Character::_name** (−1) — `string _name` declared as a private member of Hero is never set or used; the hero's name lives in `Character::_name`; this creates silent dead data through a careless naming collision

  See the snippet above under Clean Code.

## Bonus

**Bonus score: 8 / 10**

The relic system is a smart way to differentiate four heroes without a ton of code — each one comes with a unique passive that changes how you play without needing entirely separate skill trees. Clean design choice.

The armor-as-damage-buffer mechanic and the enemy skill system with probabilistic behavior (defend, skill, or attack) make combat feel less predictable than the typical random-damage loop. Five enemies across three floors with a named boss gives the whole thing a proper arc.

**Clean Code note:** The overall file structure is clean and the separation of concerns across Hero, Enemy, Location, Item, and Inventory is well done. Function sizes are mostly good — battleLoop(), enemiesAction(), playerAttack(), and useRelic() are all focused and appropriately short. The main issues to address are the getInt() function which has an unreachable error-recovery branch due to operator precedence (the `if (!cin >> choice)` should be `if (!(cin >> choice))`), and the Hero::\_name member that silently shadows Character::\_name without ever being used. Switching from `using namespace std` to explicit `std::` prefixes in the .cpp files would also be a straightforward improvement.
