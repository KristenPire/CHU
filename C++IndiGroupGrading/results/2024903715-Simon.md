# 2024903715 — Simon

## Final Grade

|           |         |
| --------- | ------- |
| Score     | 90      |
| Bonus     | 8 / 10  |
| **Final** | **98**  |

---

## Student Info

| Field           | Value                                                                  |
| --------------- | ---------------------------------------------------------------------- |
| Repository      | https://gitee.com/simon-nian/text-rpg-simon-nian2024903715             |
| Student         | 2024903715 — Simon                                                     |
| Game Score      |                                                                        |
| Deadline status | ON TIME (last commit: 2026-04-24 20:27:25 +0800)                       |
| Compilation     | PASS (cmake)                                                           |

---

## Section 1 — Subject Requirements: 30 / 30

### 1.1 Compilation

PASS

> Builds cleanly via cmake with no warnings. No external libraries used. `Character.hh` was modified by the student to add a virtual destructor and a pure virtual `getRole() const = 0` — this is noted but does not break the required interface; it enables the inheritance hierarchy.

### 1.2 Required Classes: 15 / 15

| Class         | Score | Notes                                                                                                                                                              |
| ------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Character     | 3 / 3 | Character.cpp complete; all required getters, `isAlive()`, `takeDamage()` with clamp, `heal()` with maxHP cap. `operator<<` at Character.cpp:74 prints name, HP/maxHP, ATK, DEF. Note: Character.hh modified to add `virtual ~Character()` and pure virtual `getRole()`. |
| Item          | 3 / 3 | Item.cpp complete. `operator==` at Item.cpp:40 compares name_, type_, value_. `operator<<` at Item.cpp:47 prints name, type label, and value. Extra `getTypeName()` helper added. |
| Inventory     | 3 / 3 | Inventory.cpp has addItem(), size(), isEmpty(), getItem(), removeItem(), operator<<. Full canonical form present and correct.                                      |
| Game + run()  | 3 / 3 | Game.hh declares run(); main.cpp unmodified.                                                                                                                       |
| End condition | 3 / 3 | Three distinct outcomes: win (defeat Last Driver, Game.cpp:73-83), escape (flee during combat, Game.cpp:85-88), death (Game.cpp:91-93). Each has a unique narrative message. |

### 1.3 Repository & README: 10 / 10

| Item                                 | Score | Notes                                                                                      |
| ------------------------------------ | ----- | ------------------------------------------------------------------------------------------ |
| Repo is public on Gitee              | 2 / 2 |                                                                                            |
| README.md exists                     | 2 / 2 |                                                                                            |
| Student ID listed                    | 2 / 2 | `<2024903715>` present.                                                                    |
| Game description                     | 2 / 2 | Story and mechanics clearly described; "File Structure" and "OOP Features Used" sections included. |
| Enemies, items, end condition listed | 2 / 2 | All three enemies named; all three items (Bandage/Flashlight/Old ID Card) named with types; all three end conditions (win/lose/escape) listed. |

### 1.4 Game Mechanics: 5 / 5

| Item                          | Score | Notes                                                                                                                  |
| ----------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------- |
| Hero takes damage and can die | 2 / 2 | `player_.takeDamage(damage)` at Game.cpp:227; death check at Game.cpp:67 breaks the encounter loop and triggers game-over. |
| At least one enemy            | 2 / 2 | Three enemies (Lost Passenger, Track Inspector, Last Driver) defined inline in encounters_ (Game.cpp:22-56).           |
| At least one usable item      | 1 / 1 | Bandage (Potion/heal), Flashlight (Weapon/attack boost), Old ID Card (Armor/defense boost) — all handled in useItem() (Game.cpp:235). |

---

## Section 2 — Clean Code: 34 / 40

### 2.1 Function Size & Focus: 8 / 12

`run()` (Game.cpp:59, ~35 lines) is clean and high-level — calls `showIntro()`, iterates encounters via `playEncounter()`, and outputs end messages without any inline combat logic. Supporting helpers are well-sized: `showIntro()` (22 lines), `showLocation()` (10 lines), `showStatus()` (16 lines), `readChoice()` (23 lines), and `rollDamage()` (4 lines) are all focused and single-purpose.

`playEncounter()` (Game.cpp:149, ~83 lines) is the main concern — it handles player input dispatch, player turn resolution, post-turn enemy-alive check, enemy turn, and loot reward all inline in one loop. Extracting `resolvePlayerTurn()` and `resolveEnemyTurn()` would bring each to under 30 lines. `useItem()` (Game.cpp:235, ~45 lines) is borderline — item selection and all three item-type effects are handled sequentially, which is readable but could be split into `applyItemEffect()`.

### 2.2 Canonical Form: 10 / 10

Inventory uses `std::vector<Item> items_` (Inventory.hh:26). Copy constructor at Inventory.cpp:7 uses member initializer `items_(other.items_)` — correct deep copy via vector's copy constructor. Copy-assignment at Inventory.cpp:12 has a self-assignment guard (`if (this != &other)`, line 14) and returns `*this`. Destructor at Inventory.cpp:21 is empty and correct — vector handles deallocation. All three canonical members are present and correctly implemented.

### 2.3 Naming: 10 / 10

Consistent trailing-underscore convention for all member variables (`name_`, `hp_`, `items_`, `player_`, `encountered_`). Function names read like actions: `playEncounter`, `showLocation`, `showStatus`, `showIntro`, `useItem`, `readChoice`, `rollDamage`. Local variables are descriptive throughout: `damage` (Game.cpp:161), `choice` (Game.cpp:158), `picked` (Game.cpp:253), `usedItem` (Game.cpp:175). Boolean members `won_` and `escaped_` are unambiguous. Encounter struct fields (`hasReward`, `victoryLine`, `firstSightLine`, `rewardStoryLine`) are self-documenting. No single-letter variables outside loop counters.

### 2.4 Style & Readability: 6 / 8

Consistent 4-space indentation throughout. No `using namespace std`. No dead code. Magic numbers without named constants: player initial stats `("Mina", 28, 7, 2)` at Game.cpp:9, all enemy stats inline in encounters_ (e.g. `Enemy("Lost Passenger", 14, 5, 1)` at Game.cpp:26), the defend bonus `damage -= 2` at Game.cpp:221, and the damage swing `std::rand() % 3` at Game.cpp:308. A `DEFEND_BONUS`, `DAMAGE_SWING`, and per-enemy constants would make balance tuning straightforward.

---

## Section 3 — Operator Overloading: 8 / 10

### 3.1 Required Operators: 8 / 8

| Operator               | Score | Notes                                                                                                 |
| ---------------------- | ----- | ----------------------------------------------------------------------------------------------------- |
| operator<< (Character) | 2 / 2 | Character.cpp:74 — prints `name (HP x/maxHP, ATK x, DEF x)`. Clear and informative.                  |
| operator== (Item)      | 2 / 2 | Item.cpp:40 — compares name_, type_, and value_; correct bool return.                                |
| operator<< (Item)      | 2 / 2 | Item.cpp:47 — prints `name [TypeLabel: value]`. Uses `getTypeName()` for readable type.               |
| operator<< (Inventory) | 2 / 2 | Inventory.cpp:61 — numbered list using Item's `operator<<`; prints "Empty" for empty inventory.      |

### 3.2 Bonus Overloads: 0 / 2

No additional operator overloads implemented.

Two additional points were available for any operator that fits naturally in your code — `operator+=` on Inventory to add an item (`inventory_ += reward`), `operator-=` to remove by index, or `operator<` on Item to compare by value for sorting the inventory display are all natural choices here.

---

## Section 4 — Heap / Dynamic Memory: 10 / 10

> Mode: STL ONLY

Inventory uses `std::vector<Item> items_` (Inventory.hh:26). No raw `new`/`delete` anywhere in the codebase. Canonical form is complete and correct (see §2.2). `player_` and all enemies (stored in the `Encounter` struct's `enemy` field) are held by value in Game — no unnecessary pointer indirection. `encounters_` is a `std::vector<Encounter>` populated in the constructor. Containers are used appropriately throughout.

---

## Section 5 — Inheritance: 8 / 10

> Inheritance used: YES

### 5.1 Design Quality: 4 / 5

`Player : public Character` and `Enemy : public Character` both model genuine is-a relationships. `Character` is made abstract via `virtual std::string getRole() const = 0` (Character.hh:21), which correctly forces both subclasses to identify themselves. The base class interface is clean and the virtual destructor is correctly placed. The hierarchy is used meaningfully in `showStatus(const Character& enemy)` (Game.hh:41 / Game.cpp:132) — this method accepts both Player and Enemy by reference without knowing the concrete type, which is genuine polymorphic dispatch. The limitation is that `getRole()` is the only virtual method; enemy attack behavior and the full combat resolution live in Game.cpp rather than in the subclass hierarchy, so the design is genuine but thin.

### 5.2 Technical Correctness: 4 / 5

| Item                  | Score | Notes                                                                                                              |
| --------------------- | ----- | ------------------------------------------------------------------------------------------------------------------ |
| Virtual destructor    | 2 / 2 | `virtual ~Character()` at Character.hh:10. Correct.                                                               |
| override keyword      | 0 / 1 | `getRole()` in Player.hh:9 and Enemy.hh:9 are declared without `override`. Adding `override` would catch accidental signature mismatches at compile time. |
| No object slicing     | 2 / 2 | `showStatus()` takes `const Character&` (Game.hh:41); enemies are stored in the Encounter struct by value as `Enemy` — no slicing occurs because they are never handled via a `Character` value copy. |

---

## Scoring Summary

| Section                  | Score  | Max     |
| ------------------------ | ------ | ------- |
| 1. Subject Requirements  | 30     | 30      |
| 2. Clean Code            | 34     | 40      |
| 3. Operator Overloading  | 8      | 10      |
| 4. Heap / Dynamic Memory | 10     | 10      |
| 5. Inheritance           | 8      | 10      |
| **Total**                | **90** | **100** |

---

## AI Usage

**AI_USAGE: 2 / 5** — The overall architecture is cleaner than average for this course level and `readChoice()` (Game.cpp:281) uses `std::stringstream` with a trailing-char check to reject garbage input — a robust technique unusual at this level — but the complete absence of code comments (where AI output typically adds explanations), the organic magic numbers scattered throughout (`damage -= 2` at Game.cpp:221, player stats `28,7,2` at Game.cpp:9), and the `return 0;` instead of `nullptr` at Inventory.cpp:44 are all consistent with organic student work.

- `Game.cpp:281` — `std::stringstream` + `char extra` input validation is more sophisticated than typical student work↑
- `Game.cpp:9` — raw stats `("Mina", 28, 7, 2)` with no named constants; organic student shorthand↓
- `Inventory.cpp:44` — `return 0;` instead of `nullptr`; organic mistake inconsistent with AI output↓
- `Game.cpp:221` — hardcoded `damage -= 2` for defend stance without a named constant; organic↓

## Bonus

**Bonus score: 8 / 10**

**Comment:**

The atmospheric setting carries real weight — a 00:43 shift at a closed station, three distinct locations (Platform, Tunnel, Last Carriage) each with their own intro line and an enemy that fits the space. The narrative voice for Mina is consistent throughout: "That wasn't a passenger... not anymore." This is a game that knows what it is.

The `Encounter` struct (Game.hh:17-28) is the standout architectural choice: every encounter is a complete data record with location, intro text, enemy, victory line, reward, and story lines. `run()` and `playEncounter()` operate on that data without needing to know the specifics of each fight — adding a fourth encounter would require zero logic changes. That's real design thinking.

The free-turn item mechanic (choice 2 calls `continue` at Game.cpp:180, skipping the enemy turn) is a small but meaningful game design decision — items are a genuine strategic tool rather than just another action that costs a turn. The three-way outcome (win / flee / die) with distinct narrative endings adds stakes to the escape option.

**Clean Code note:** The codebase is very clean — consistent `name_` trailing-underscore convention throughout, no `using namespace std`, `run()` reads like a high-level description of the game loop. The main thing to address is `playEncounter()` at ~83 lines: extracting `resolvePlayerTurn()` and `resolveEnemyTurn()` as helpers would bring it in line with the care shown everywhere else. Also, named constants for the player stats, enemy stats, and the defend bonus (currently `damage -= 2` at Game.cpp:221) would make balance tuning straightforward.
